# 算法效果验证平台（AlgoEval）产品需求文档

> 版本：v1.0 ｜ 撰写日期：2026-07-08
> 文档状态：评审稿
> 关联文档：《大模型图片描述质量度量方案（标注实操版）》（本仓库 `image-caption-metrics-design.md`，作为平台"评估标准模板库"的首个内置模板）

---

## 一、背景与目标

### 1.1 背景

算法从"需求提出"到"模型上线"的全生命周期中，效果验证长期存在以下痛点：

| 阶段 | 现状痛点 |
|------|---------|
| 需求期 | 产品只有规则/需求文档，没有模型，无法提前预估算法可行性与效果上限；只能等算法排期后才知道"值不值得做" |
| 开发期 | 算法交付 SDK / 网页 demo 后，测试靠人肉拉表打分，无统一指标口径，主观分与准召指标割裂 |
| 验收期 | 模型测试依赖人工逐条核对，周期长、成本高；缺少"AI 先打分 + 人工抽检"的分层机制 |
| 上线前 | 缺少真实用户（含海外用户）满意度数据支撑上线决策 |
| 全程 | 标注、评估、报告分散在表格和 IM 里，不可追溯、不可复用 |

### 1.2 产品目标

搭建一个**一站式算法效果验证平台**，覆盖"需求期预验证 → 开发期自动化测试验收 → 上线前用户满意度评测"三个阶段，并内置**自动化测试标注平台**作为人工环节的统一承载：

1. **需求期**：基于产品规则/需求文档/算法 SDK/网页 demo，用平台上传的数据 + 大模型进行初期效果验证，输出主观打分结论与准召等指标，自动生成评估报告。
2. **开发期/验收期**：模型开发完成后，自动化执行模型测试——被测模型批量输出 → 云端 API 评估模型（judge 小模型）打分 → 人工校验 AI 打分准确性 → 自动生成模型测试报告。
3. **上线前**：将需求生成的结果邀请外部用户/海外用户打分，评估用户满意度，输出满意度报告。
4. **贯穿全程**：自动化测试标注平台，支持任务分发、标注、质检、仲裁与一致性统计。

### 1.3 成功指标（North Star & 过程指标）

| 指标 | 目标（上线 3 个月） |
|------|-------------------|
| 需求期预验证周期 | 从平均 2 周 → ≤ 2 天 |
| 模型验收人工工作量 | 人工全量核对 → AI 打分 + ≤ 20% 人工抽检 |
| AI 打分与人工校验一致率 | ≥ 90%（Cohen's Kappa ≥ 0.75） |
| 报告自动化率 | 三类报告 100% 平台自动生成 |
| 平台月活评估任务数 | ≥ 50 个 |

### 1.4 用户角色

| 角色 | 职责 | 核心诉求 |
|------|------|---------|
| 产品经理 | 提交需求/规则，查看评估与满意度报告 | 快速知道"这个算法需求可行吗、效果多好" |
| 算法工程师 | 接入 SDK/API/模型，查看指标明细与 badcase | 定位问题样本，对比版本效果 |
| 测试/评估工程师 | 配置评估任务、维护指标口径、组织人工校验 | 自动化、口径统一、可追溯 |
| 标注员/质检员 | 在标注平台完成打分、校验、质检 | 界面高效、规则清晰、有争议可仲裁 |
| 外部/海外用户 | 通过邀请链接对结果打分 | 无需登录、移动端友好、多语言 |
| 管理员 | 权限、模型接入、数据安全管理 | 数据隔离、成本可控 |

---

## 二、产品架构

### 2.1 功能架构图

```
┌─────────────────────────── 算法效果验证平台 ───────────────────────────┐
│                                                                        │
│  ┌──────────┐  ┌──────────────┐  ┌──────────────┐  ┌────────────────┐  │
│  │ M1 需求与 │  │ M3 初期效果  │  │ M4 模型测试   │  │ M5 用户满意度  │  │
│  │ 被测对象  │→│ 验证(LLM评估)│  │ 与自动化验收  │  │ 评测           │  │
│  │ 管理     │  └──────┬───────┘  └──────┬───────┘  └───────┬────────┘  │
│  └──────────┘         │                 │                  │           │
│  ┌──────────┐         ▼                 ▼                  ▼           │
│  │ M2 数据集 │  ┌─────────────────────────────────────────────────┐    │
│  │ 管理     │→│        M6 自动化测试标注平台（人工环节统一承载）    │    │
│  └──────────┘  └─────────────────────────────────────────────────┘    │
│                        │                 │                  │          │
│                        ▼                 ▼                  ▼          │
│                ┌─────────────────────────────────────────────────┐    │
│                │   M7 报告中心（评估报告 / 测试报告 / 满意度报告）   │    │
│                └─────────────────────────────────────────────────┘    │
│  底座：M0 模型接入与编排（大模型/评估小模型/被测SDK·API·网页demo驱动）    │
│        权限与租户 ｜ 任务调度 ｜ 存储 ｜ 审计日志 ｜ 成本看板             │
└────────────────────────────────────────────────────────────────────────┘
```

### 2.2 核心流程（三条主链路）

**链路 A｜需求期预验证**
`创建需求(规则/文档/SDK/demo) → 绑定数据集 → 选择评估模板与大模型 → 批量执行 → LLM 主观打分 + 指标计算(准召/F1) → (可选)人工抽检 → 自动生成评估报告`

**链路 B｜模型自动化测试与验收**
`注册被测模型(API/SDK) → 绑定验收数据集与验收标准 → 被测模型批量输出 → 云端评估小模型(judge)逐条打分 → 按抽样策略派发人工校验任务 → 计算 AI-人工一致率 → 达标则自动生成测试报告并给出验收结论`

**链路 C｜用户满意度评测**
`选择结果集 → 创建评测活动(问卷/打分维度/多语言) → 生成邀请链接/二维码 → 外部·海外用户匿名打分 → 实时回收统计(CSAT/NPS/维度分) → 自动生成满意度报告`

---

## 三、功能需求详述

### M0 模型接入与编排（底座）

| 编号 | 需求 | 说明 | 优先级 |
|------|------|------|:---:|
| M0-1 | 大模型接入 | 支持接入主流云端大模型 API（OpenAI/Anthropic/Qwen/GLM 等），统一密钥管理、限流与重试 | P0 |
| M0-2 | 评估小模型接入 | 支持注册专用 judge 模型（云端 API），可配置打分 prompt 模板、输出 schema（分数+理由+置信度） | P0 |
| M0-3 | 被测对象驱动 | 三种驱动方式：① REST API；② SDK（沙箱容器内调用，提供 Python/Java 适配脚手架）；③ 网页 demo（无 API 时通过浏览器自动化 Playwright 驱动，录制"输入→提取输出"脚本） | P0（③为 P1） |
| M0-4 | 任务调度 | 批量任务队列、并发控制、断点续跑、失败重试、进度实时推送 | P0 |
| M0-5 | 成本看板 | 按任务统计 token 消耗与调用费用，支持预算上限熔断 | P1 |

### M1 需求与被测对象管理

| 编号 | 需求 | 说明 | 优先级 |
|------|------|------|:---:|
| M1-1 | 需求录入 | 支持四类输入：产品规则（结构化规则条目）、需求文档（附件/富文本）、算法 SDK、网页 demo 链接；一个需求可关联多个被测对象版本 | P0 |
| M1-2 | 评估标准配置 | 将规则/需求转化为可执行的评估标准：维度、权重、打分档位、判定阈值。内置模板库（首个模板：图片描述质量 10 维度 100 分制），支持复制修改 | P0 |
| M1-3 | 版本管理 | 被测对象按版本管理（v1/v2…），支持版本间效果对比 | P1 |
| M1-4 | 目标设定 | 为需求设定验收目标（如 精确率≥90%、主观均分≥85），报告中自动对照 | P0 |

### M2 数据集管理

| 编号 | 需求 | 说明 | 优先级 |
|------|------|------|:---:|
| M2-1 | 数据上传 | 支持 CSV/JSONL/Excel/图片压缩包/音视频；拖拽上传+断点续传；字段映射向导（输入列/期望输出列/标签列） | P0 |
| M2-2 | 数据集类型 | ① 无标注集（仅输入，用于主观评估）；② 有标注集（含 ground truth，用于准召计算）；③ 结果集（模型输出结果，用于满意度评测） | P0 |
| M2-3 | 数据预览与抽样 | 在线预览、随机/分层抽样生成子集 | P0 |
| M2-4 | 数据安全 | 项目级隔离、脱敏标记、外发（满意度评测）需审批 | P1 |

### M3 初期效果验证（需求期 LLM 评估）

**目标**：在没有正式模型或刚有 demo 时，用"上传数据 + 大模型"快速回答两个问题——*主观上效果如何*、*客观指标能到多少*。

| 编号 | 需求 | 说明 | 优先级 |
|------|------|------|:---:|
| M3-1 | 评估任务创建 | 选择：需求（含评估标准）→ 数据集 → 被测对象（可选；无被测对象时由大模型按规则直接生成结果再自评，用于可行性探索）→ 评估大模型 → 抽样规模 | P0 |
| M3-2 | LLM 主观打分 | 大模型按评估标准逐条打分，输出：总分（0-100）、分维度得分、打分理由、置信度；低置信样本自动标记待人工复核 | P0 |
| M3-3 | 客观指标计算 | 有标注集自动计算：准确率、精确率、召回率、F1、混淆矩阵；分类任务支持逐类别指标；支持自定义匹配规则（精确/包含/语义相似度阈值） | P0 |
| M3-4 | 结论生成 | 自动输出结构化结论：主观分分布、指标达标情况（对照 M1-4 目标）、Top badcase 列表、风险提示与改进建议（LLM 生成，标注"AI 生成"） | P0 |
| M3-5 | 人工抽检入口 | 一键将低分/低置信样本推送到标注平台（M6）复核，复核结果回写并更新报告 | P1 |
| M3-6 | 评估报告 | 自动生成《算法需求初期评估报告》（模板见 M7），支持在线查看、导出 PDF/Word、分享链接 | P0 |

### M4 模型测试与自动化验收

**目标**：模型开发完成后，以"AI 打分为主、人工校验兜底"的方式自动化完成测试与验收。

**流水线**：`被测模型输出 → 评估小模型打分 → 人工校验（抽样） → 一致性计算 → 测试报告`

| 编号 | 需求 | 说明 | 优先级 |
|------|------|------|:---:|
| M4-1 | 验收任务配置 | 选择被测模型版本、验收数据集、评估小模型、打分标准、抽检策略（比例/分层/全量低分）、验收阈值 | P0 |
| M4-2 | 批量推理 | 平台调用被测模型批量产出结果，记录延迟、失败率等工程指标 | P0 |
| M4-3 | AI 自动打分 | 云端评估小模型逐条打分（分数+理由+置信度）；支持双 judge 交叉打分，分歧样本强制进人工 | P0 |
| M4-4 | 人工校验 | 按抽检策略生成校验任务派发到标注平台；校验员看到"样本 + 模型输出 + AI 打分及理由"，操作为【认可 / 修改分数 / 标记争议】 | P0 |
| M4-5 | 一致性统计 | 计算 AI 分与人工分的一致率、平均绝对偏差、Cohen's Kappa；低于阈值时告警并建议扩大抽检或优化 judge prompt | P0 |
| M4-6 | 验收判定 | 依据"最终分（人工校验过的以人工为准）"对照验收阈值，自动给出 通过/有条件通过/不通过 结论 | P0 |
| M4-7 | 测试报告 | 自动生成《模型测试验收报告》：指标总览、分维度得分、AI-人工一致性、badcase 明细、工程指标、验收结论与签署记录 | P0 |
| M4-8 | 版本对比 | 同一需求下多版本模型指标对比视图（表格+图表） | P1 |
| M4-9 | 回归测试 | 验收集固化为回归集，模型迭代时一键重跑并对比 | P1 |

### M5 用户满意度评测

**目标**：把需求生成的结果交给真实用户（含海外用户）打分，量化满意度。

| 编号 | 需求 | 说明 | 优先级 |
|------|------|------|:---:|
| M5-1 | 评测活动创建 | 选择结果集 → 配置问卷：总体满意度（1-5 星 CSAT）、NPS（0-10）、自定义维度打分、开放反馈；设置样本随机分发策略（每人 N 条） | P0 |
| M5-2 | 多语言与海外可达 | 问卷界面多语言（中/英起步，i18n 框架预留）；样本内容可选机器翻译辅助；链接海外可访问（静态化+CDN），移动端自适应 | P0 |
| M5-3 | 匿名参与 | 邀请链接/二维码免登录参与；设备指纹+频控防刷；可选受邀名单制 | P0 |
| M5-4 | 实时回收看板 | 参与人数、完成率、CSAT、NPS、维度分布实时更新；按地区/语言/渠道切片 | P0 |
| M5-5 | 满意度报告 | 自动生成《用户满意度评测报告》：样本量与置信度说明、CSAT/NPS、维度分析、开放反馈聚类摘要（LLM 生成）、与主观/客观指标的交叉对照 | P0 |
| M5-6 | 激励与合规 | 可配置抽奖/积分激励；隐私声明、数据出境合规提示 | P1 |

### M6 自动化测试标注平台

**目标**：承载所有人工环节（初验抽检、验收校验、满意度开放题整理、通用标注），并提供自动化的任务流转与质量控制。

| 编号 | 需求 | 说明 | 优先级 |
|------|------|------|:---:|
| M6-1 | 任务管理 | 任务创建（从 M3/M4/M5 自动创建或手动创建）、批次拆分、自动派发（按人均配额/技能标签）、进度看板 | P0 |
| M6-2 | 标注工作台 | 左侧样本（图/文/音视频渲染），右侧评分表单（按评估标准模板动态渲染维度与档位）；快捷键操作；单条计时 | P0 |
| M6-3 | 标注模板 | 与 M1-2 共用评估标准模板库；首发内置《图片描述质量度量方案》10 维度表单（五步法引导） | P0 |
| M6-4 | 质检与仲裁 | 抽检质检、双人背靠背标注+不一致仲裁、金标（埋点题）准确率考核 | P0 |
| M6-5 | 一致性度量 | 标注员间一致性（Kappa/Krippendorff's α）、个人准确率与效率报表 | P1 |
| M6-6 | AI 预标注 | 用大模型预填分数与理由，标注员确认/修改，提升效率（与 M4-4 复用同一交互） | P1 |
| M6-7 | 数据回流 | 标注结果一键回流为有标注数据集（M2），沉淀为回归集/训练集 | P1 |

### M7 报告中心

| 编号 | 需求 | 说明 | 优先级 |
|------|------|------|:---:|
| M7-1 | 三类报告模板 | 《初期评估报告》《模型测试验收报告》《用户满意度报告》，结构化章节 + 图表自动填充 | P0 |
| M7-2 | 报告管理 | 列表、检索、版本、在线预览、导出 PDF/Word、分享链接（可设有效期与水印） | P0 |
| M7-3 | 结论摘要 | 每份报告首页自动生成"一页纸结论"（目标对照 + 结论 + 风险 + 建议） | P0 |
| M7-4 | 跨报告对比 | 同一需求下三阶段报告串联视图，形成完整证据链 | P1 |

**报告模板章节（示例：模型测试验收报告）**

1. 一页纸结论（验收结论/目标对照/关键风险）
2. 测试对象与环境（模型版本、数据集、评估配置）
3. 指标总览（准确率/精确率/召回率/F1、主观分分布）
4. AI 打分与人工校验一致性（一致率、Kappa、偏差分析）
5. 分维度分析与 badcase Top20
6. 工程指标（时延 P50/P99、失败率、成本）
7. 结论与建议、签署记录

---

## 四、非功能需求

| 类别 | 要求 |
|------|------|
| 性能 | 批量评估任务支持 ≥ 10 万样本/任务；看板数据延迟 ≤ 5s |
| 可用性 | 核心链路可用性 99.9%；任务断点续跑 |
| 安全 | 项目级数据隔离；密钥加密存储；外发数据审批+水印；操作审计日志 |
| 合规 | 海外评测遵循 GDPR 基线：匿名化、知情同意、可删除 |
| 扩展性 | 评估模板、judge 模型、指标计算器均插件化 |
| 国际化 | 用户端问卷 i18n；管理端中文优先、预留英文 |

---

## 五、里程碑规划

| 阶段 | 范围 | 周期 |
|------|------|------|
| MVP（P0） | M0-1/2/4、M1-1/2/4、M2-1/2/3、M3 全部 P0、M4 全部 P0、M6-1/2/3/4、M7-1/2/3 | 8 周 |
| V1.1 | M5 满意度评测全链路、M0-3③ 网页 demo 驱动、M6-6 AI 预标注 | +4 周 |
| V1.2 | 版本对比、回归测试、成本看板、跨报告证据链 | +4 周 |

---

## 六、验收标准（平台自身）

1. 三条主链路（A/B/C）均可端到端跑通并产出对应报告。
2. 以内置《图片描述质量度量方案》为模板，完成一次 ≥ 500 样本的验收演练：AI 打分与人工抽检一致率 ≥ 85%。
3. 满意度评测支持中英双语问卷，海外网络可访问，回收 ≥ 100 份有效问卷并生成报告。
4. 报告三类模板均可导出 PDF 且图表数据与看板一致。

---

## 七、网页版 Demo 说明

本 PRD 附带可交互的网页版 Demo（`demo/index.html`，单文件、无外部依赖、直接浏览器打开），使用模拟数据演示以下页面：

1. **工作台**：核心指标卡片、评估任务趋势、最近报告
2. **需求管理**：四类接入方式的需求列表与新建入口
3. **数据集管理**：上传区与数据集列表
4. **初期效果验证**：任务配置 → 模拟运行 → 主观分分布 + 准召指标 → 生成评估报告
5. **模型测试与验收**：四阶段流水线、AI 打分 + 人工校验交互、一致性统计
6. **用户满意度评测**：活动配置、邀请链接、CSAT/NPS 实时看板
7. **标注平台**：标注任务列表 + 标注工作台（内置图片描述 10 维度模板）
8. **报告中心**：三类报告列表与在线预览

Demo 仅为交互与信息架构演示，不包含真实模型调用。
