# AI 自动化标注测试平台 —— 角色诉求梳理

> 版本：v1.0 ｜ 阶段：需求收集
> 目的：以「AI 产品经理」为范例深度，把平台六类角色的核心诉求逐一展开为：角色画像 → 诉求拆解 → 典型场景（用户故事）→ 功能需求（含优先级）→ 成功指标 → 当前痛点，作为后续 PRD 与排期的输入。

---

## 0. 角色总览

| 角色 | 一句话诉求 | 在平台中的定位 |
|------|-----------|--------------|
| AI 产品经理 | 低成本验证需求实现效果 | 需求发起方、效果验收方 |
| 算法工程师 | 稳定复现实验、发现切片问题 | 模型/算法供给方、实验主体 |
| 算法测试 / 质量负责人 | 按门禁验收并留档 | 质量把关方、发布卡点 |
| 标注 / 运营 | 高效、可靠地复核争议样本 | 数据生产与质量执行方 |
| 外部受访者 | 清晰完成体验评估并获得激励 | 外部数据/主观评价供给方 |
| 平台管理员 | 安全、成本、租户和资源治理 | 平台运营与治理方 |

六个角色围绕同一条主链路协作：

```
产品经理定义需求/评测标准
   → 算法工程师接入模型跑实验
      → 标注/运营 + 外部受访者产出标注与主观评价
         → 测试/质量负责人按门禁验收留档
            → 平台管理员保障安全、成本与合规
```

---

## 1. AI 产品经理（范例角色，展开最细）

### 1.1 角色画像

- 负责一个或多个 AI 功能（如图片描述、智能问答、内容审核）的产品定义与效果验收；
- 懂业务、懂指标，但**不写代码、不搭环境**；
- 工作节奏是"快速试错"：一个想法能不能做、做到什么程度，希望几天内有答案，而不是等排期两周拿到一个 demo；
- 是评测结论的第一消费者，也要拿评估报告向上汇报、向业务方交代。

### 1.2 核心诉求拆解：「低成本验证需求实现效果」

| 子诉求 | 展开说明 | 背后动机 |
|--------|---------|---------|
| ① 零代码搭建概念验证 demo | 在界面上用规则/逻辑编排 + 选择现成模型/算法能力，拼出一个可运行的 PoC，不依赖工程排期 | 把"可行性验证"从周级压缩到天级/小时级 |
| ② 自带数据快速试 | 能上传/导入自己的业务数据（Excel、CSV、图片包、线上抽样），直接在真实数据上看效果 | 公开数据集上的效果说服不了业务方，必须用自己的数据 |
| ③ 直观看到输出效果 | 逐条查看模型输入/输出对照，支持抽样浏览、按好坏筛选 | 指标是抽象的，PM 需要"眼见为实"来建立判断 |
| ④ 一眼看懂的指标 | 平台给出准确率、覆盖率、幻觉率等指标的可视化看板，指标定义有解释，不需要问算法同学"这个数什么意思" | 降低理解门槛，让 PM 能独立做 go/no-go 判断 |
| ⑤ 自动生成评估报告 | 一键导出结构化评估报告（数据集说明、指标结果、典型 case、结论建议），可直接用于汇报与立项 | 省去手工整理截图、拼 PPT 的重复劳动，且报告口径统一 |
| ⑥ 需求与评测标准可沉淀 | PoC 中定义的规则、评分标准（如"图片描述质量 100 分制"）能沉淀为平台资产，后续正式立项直接复用 | 避免 PoC 与正式研发"两张皮"，验证标准即验收标准 |

### 1.3 典型场景（用户故事）

1. **可行性验证**：作为 PM，我想把 500 张业务图片导入平台，选一个 VLM 模型跑图片描述，配置好评分规则后自动出分，以便在立项评审前回答"现有模型能做到多少分"。
2. **方案对比**：作为 PM，我想用同一批数据同时跑两个候选模型/两版 prompt，并排看指标和逐条输出，以便选择性价比更高的方案。
3. **汇报留档**：作为 PM，我想在验证结束后一键生成评估报告（含数据说明、指标、典型好/坏 case），以便直接放进立项材料，且三个月后还能回溯当时的结论依据。
4. **标准交接**：作为 PM，我想把 PoC 里调好的评分规则一键转成正式评测模板，交给测试/质量负责人作为门禁标准，以便验证口径和验收口径一致。

### 1.4 功能需求

| 优先级 | 功能 | 说明 |
|-------|------|------|
| P0 | 可视化 PoC 编排 | 规则/逻辑/算法能力的低代码编排；内置常用模型与能力组件 |
| P0 | 数据导入 | 支持 CSV/Excel/图片压缩包/JSONL；导入时字段映射与抽样预览 |
| P0 | 逐样本效果查看 | 输入-输出对照视图，支持筛选（按得分、按错误类型）、抽样、标星收藏典型 case |
| P0 | 指标看板 | 任务级指标卡片 + 分布图；每个指标附口径说明 |
| P0 | 自动评估报告 | 模板化生成，支持导出 PDF/Word/在线链接分享 |
| P1 | 多方案 A/B 对比 | 同一数据集下多模型/多 prompt 并排对比 |
| P1 | 评测标准资产化 | 评分规则模板库，可版本化、可交接给质量负责人 |
| P2 | 成本预估 | 任务发起前预估调用费用与耗时 |

### 1.5 成功指标

- 一次 PoC 验证从发起到出报告 ≤ 1 天（现状：依赖工程排期，1–2 周）；
- PoC 全流程 PM 可自助完成，0 次工程介入；
- 评估报告可被直接用于立项/汇报的比例 ≥ 80%。

### 1.6 当前痛点（平台要解决的）

- 想验证一个想法必须找算法同学写脚本、借 GPU，排期长、沟通成本高；
- 效果好坏靠算法同学口头转述或零散截图，没有统一指标口径；
- 每次评估报告手工整理，格式不一，历史结论无法追溯；
- PoC 阶段的评分标准没有沉淀，正式研发时重新定义一遍，前后口径对不上。

---

## 2. 算法工程师

### 2.1 角色画像

- 负责模型选型、微调、prompt 工程与推理服务；
- 日常在代码/notebook 里工作，希望平台提供 **SDK/API 而非强制走界面**；
- 最怕两件事：实验结果复现不出来、上线后才发现某类样本（切片）效果崩了。

### 2.2 核心诉求拆解：「稳定复现实验、发现切片问题」

| 子诉求 | 展开说明 | 背后动机 |
|--------|---------|---------|
| ① 实验全要素固化 | 每次评测运行自动记录：模型版本、权重/端点、prompt、推理参数（temperature 等）、数据集版本、评测代码版本 | 任何一次历史结果都能一键重跑复现，排查"分数怎么掉了" |
| ② SDK/API 优先 | 用几行代码把自己的模型/服务接入评测流水线，在 CI 或 notebook 里触发评测 | 融入现有开发工作流，而不是打断它 |
| ③ 逐样本错误分析 | 能下钻到每一条 bad case，看到输入、输出、期望、评分明细与失败原因分类 | 指标下降 0.5 分不可行动，"哪 30 条错了、错在哪类"才可行动 |
| ④ 数据切片评估 | 按属性（如图片清晰度、语言、类目、长度）切片看指标，自动发现"整体达标但某切片崩了" | 平均分掩盖长尾问题，切片是发现回归的关键手段 |
| ⑤ 版本对比 | 两个模型版本在同一数据集上 diff：指标 diff + 样本级 diff（哪些从对变错、从错变对） | 判断新版本是"净提升"还是"按下葫芦浮起瓢" |
| ⑥ 推理配置管理 | 推理参数、并发、超时、重试在平台统一配置和记录 | 排除"参数不一致"这一最常见的复现失败原因 |

### 2.3 典型场景（用户故事）

1. 作为算法工程师，我想在训练脚本末尾调用平台 SDK 自动提交评测，以便每个 checkpoint 都有可比的分数。
2. 作为算法工程师，我想对比 v1.3 和 v1.4 在同一测试集上的样本级 diff，重点看"由对变错"的样本，以便确认新版本没有引入回归。
3. 作为算法工程师，我想按"图片含文字/不含文字"切片看幻觉率，以便定位 OCR 场景的专项问题。
4. 作为算法工程师，我想一键重跑三个月前的某次评测并得到一致结果，以便回应"当时的结论还成立吗"。

### 2.4 功能需求

| 优先级 | 功能 |
|-------|------|
| P0 | Python SDK + REST API：提交评测、拉取结果、查询状态 |
| P0 | 运行快照：模型/数据/参数/代码版本全记录，支持一键重跑 |
| P0 | 逐样本结果浏览与错误归类标签 |
| P0 | 版本对比视图（指标 diff + 样本级 diff） |
| P1 | 切片分析：按元数据字段自动分组出指标，支持自定义切片 |
| P1 | 推理配置模板（参数、并发、重试策略） |
| P2 | 与 CI/CD 集成的 webhook / 状态回调 |

### 2.5 成功指标

- 任意历史评测可复现，指标偏差 ≤ 可解释范围（如采样随机性）；
- 从"发现指标下降"到"定位到具体切片和 bad case"≤ 30 分钟；
- 接入一个新模型到跑通第一次评测 ≤ 半天。

### 2.6 当前痛点

- 实验记录散落在个人 notebook 和聊天记录里，换个人就复现不了；
- 只有整体平均分，切片问题上线后才被业务投诉暴露；
- 对比两个版本要手工拼表格、肉眼 diff 样本。

---

## 3. 算法测试 / 质量负责人

### 3.1 角色画像

- 对"能不能上线"负责，需要一套**客观、固定、可审计**的验收流程；
- 关注的不是单次分数，而是标准是否被一致执行、记录是否完整可追溯；
- 常要向上级/审计方证明"当时是按标准放行的"。

### 3.2 核心诉求拆解：「按门禁验收并留档」

| 子诉求 | 展开说明 | 背后动机 |
|--------|---------|---------|
| ① 基准测试集管理 | 维护版本化的验收测试集（含冻结、防泄漏、定期换血机制） | 测试集被训练数据污染或随意改动，验收就失去意义 |
| ② 门禁规则配置 | 把验收标准配置成机器可执行的门禁：如"总分 ≥ 85 且 幻觉率 ≤ 2% 且 任一关键切片不低于基线 -1 分" | 把"拍脑袋验收"变成规则化验收，减少扯皮 |
| ③ 一键触发回归 | 新版本提测时，自动在全部基准集上跑回归并出门禁判定 | 验收动作标准化、省人力 |
| ④ 签收 / 驳回工作流 | 门禁通过后仍需人工签收确认；驳回时必须填原因并通知提测方 | 机器判定 + 人工负责制，责任到人 |
| ⑤ 全程留档 | 每次验收的测试集版本、门禁规则版本、结果、签收人、时间戳不可篡改地存档 | 满足审计与事后追责需要 |
| ⑥ 基线追踪 | 维护每个能力的历史基线曲线，防止"温水煮青蛙"式缓慢劣化 | 单次对比看不出长期趋势 |

### 3.3 典型场景（用户故事）

1. 作为质量负责人，我想为"图片描述"能力配置门禁（测试集 v3 + 总分 ≥ 85 + 幻觉率 ≤ 2%），以便所有版本按同一把尺子验收。
2. 作为质量负责人，我想在算法提测后收到自动回归结果和门禁判定，只需复核异常项并签收，以便把验收耗时从两天缩短到两小时。
3. 作为质量负责人，我想驳回一个"总分达标但 OCR 切片回归"的版本，并附上驳回原因与证据样本，以便提测方明确知道要修什么。
4. 作为质量负责人，我想在审计时导出过去一年所有版本的验收记录，以便证明发布流程合规。

### 3.4 功能需求

| 优先级 | 功能 |
|-------|------|
| P0 | 测试集版本管理（冻结、权限隔离、变更审批） |
| P0 | 门禁规则引擎：多指标组合条件、切片级条件、对比基线条件 |
| P0 | 回归任务：手动/提测事件触发，自动出门禁判定 |
| P0 | 签收/驳回工作流（附原因、证据样本、通知） |
| P0 | 验收档案：不可篡改留档 + 检索导出 |
| P1 | 基线趋势看板与劣化告警 |
| P2 | 与发布系统联动（门禁不过阻断发布） |

### 3.5 成功指标

- 100% 的发布版本有完整验收档案；
- 单次验收人工耗时下降 ≥ 70%；
- 因验收标准不一致引发的争议为 0。

### 3.6 当前痛点

- 验收标准在文档里，执行靠人，不同人尺度不一；
- 回归靠手工跑脚本 + 邮件传报告，留档零散；
- 出了线上问题回查时，找不到"当时是谁、按什么标准放行的"。

---

## 4. 标注 / 运营

### 4.1 角色画像

- 管理内外部标注团队，负责标注任务的分发、培训、质检、验收与产能保障；
- 核心矛盾：**质量与产能的平衡**——既要标得准，又要标得快、标得起（成本）；
- 大量时间耗在处理"标注员之间意见不一致"的争议样本上。

### 4.2 核心诉求拆解：「高效、可靠地复核争议样本」

| 子诉求 | 展开说明 | 背后动机 |
|--------|---------|---------|
| ① 任务全生命周期管理 | 建任务、拆包分发、进度跟踪、验收结算一站式完成 | 目前靠表格 + 群消息管理，易漏易错 |
| ② 培训与准入考核 | 新标注员先学规则文档/示例，通过金标考试才能上岗 | 从源头减少低质标注 |
| ③ 金标（Golden Set）质检 | 在任务中埋入已知答案样本，自动计算每人准确率，低于阈值自动预警/停派 | 用抽样机制持续、低成本地监控质量 |
| ④ 多人标注 + 自动仲裁路由 | 关键任务多人背靠背标注，一致则通过；不一致自动进入仲裁队列，由资深标注员/专家裁决 | 争议样本是质量瓶颈，必须有专门流水线而不是线下吵 |
| ⑤ 仲裁工作台 | 仲裁界面并排展示各方标注结果与依据、原始数据、规则条款引用，裁决结果反哺规则 FAQ | 让复核"快而有据"，并让规则越用越清晰 |
| ⑥ 质量与产能双看板 | 人均产能、单价成本、一次通过率、返工率、仲裁率按人/按团队/按任务可视化 | 数据化管理团队，识别需培训或淘汰的人员 |

### 4.3 典型场景（用户故事）

1. 作为运营，我想把 10 万条样本按 500 条一包分发给 3 家供应商，并实时看到各家进度与质检通过率，以便动态调整派单量。
2. 作为运营，我想让所有争议样本（双标不一致）自动流入仲裁队列并按优先级排序，以便资深同学集中批量裁决，而不是在群里逐条讨论。
3. 作为运营，我想看到某标注员金标准确率连续两天低于 90% 时收到告警并自动暂停给他派单，以便及时止损。
4. 作为运营，我想把高频争议点整理成规则 FAQ 并推送给全员，以便同类争议不再重复发生。

### 4.4 功能需求

| 优先级 | 功能 |
|-------|------|
| P0 | 任务管理：创建、拆包、分发、进度、验收 |
| P0 | 金标机制：埋点、自动判分、个人准确率统计与告警 |
| P0 | 多人标注一致性计算 + 争议样本自动进仲裁队列 |
| P0 | 仲裁工作台（并排对比、规则引用、批量裁决） |
| P1 | 培训考核模块（规则学习 + 准入考试） |
| P1 | 质量/产能/成本看板 |
| P2 | 供应商/团队结算对账支持 |

### 4.5 成功指标

- 争议样本平均处理时长 ≤ 10 分钟/条（现状：线下沟通，小时级）；
- 交付数据一次验收通过率 ≥ 95%；
- 质检人力占比下降（金标自动质检替代大部分人工抽检）。

### 4.6 当前痛点

- 任务派发和进度靠 Excel 和群消息，口径混乱；
- 争议样本靠群里@人讨论，慢且无留痕，同样的争议反复出现；
- 标注员质量参差不齐，缺乏客观、持续的个人质量数据。

---

## 5. 外部受访者

### 5.1 角色画像

- 平台外部的普通用户/领域专家，受邀参与体验评估、主观评分、盲测偏好投票等；
- 非专业人员，可能在手机上、碎片时间参与；
- 关心三件事：**任务清不清楚、麻不麻烦、钱什么时候到**。

### 5.2 核心诉求拆解：「清晰完成体验评估并获得激励」

| 子诉求 | 展开说明 | 背后动机 |
|--------|---------|---------|
| ① 低门槛受邀访问 | 点开邀请链接即可参与，无需下载 App 或复杂注册，支持手机端 | 每多一步注册流程就流失一批受访者 |
| ② 清晰的知情同意 | 用平实语言说明数据用途、匿名化方式、权利（可退出、可删除），逐项授权 | 合规要求（个保法/GDPR），也是建立信任的前提 |
| ③ 任务说明与示例 | 答题前有简明指引和好/坏示例，题目本身表述无歧义 | 受访者理解偏差会直接污染数据 |
| ④ 顺滑的答题体验 | 进度条、自动保存、断点续答、预计剩余时长 | 减少中途放弃率 |
| ⑤ 进度与激励透明 | 随时可查：已完成多少、审核状态、预计报酬、发放时间与方式 | "钱不透明"是外部受访最大的信任杀手 |
| ⑥ 申诉渠道 | 对"答题被判无效、报酬被扣"可提出申诉并得到回复 | 保护受访者，也倒逼质检规则公正 |

### 5.3 典型场景（用户故事）

1. 作为受访者，我想在手机上点开链接、阅读同意书并授权后直接开始答题，以便利用通勤时间参与。
2. 作为受访者，我想在答题中途退出后下次从断点继续，以便不必一次做完 50 题。
3. 作为受访者，我想在个人页看到"已完成 30 题 / 审核通过 28 题 / 预计报酬 56 元 / 下周三发放"，以便对激励有明确预期。
4. 作为受访者，我想对被判无效的 2 题提交申诉，以便挽回被误判的报酬。

### 5.4 功能需求

| 优先级 | 功能 |
|-------|------|
| P0 | 邀请链接/邀请码访问，移动端自适应 H5 |
| P0 | 知情同意与授权流程（版本化的同意书、可撤回） |
| P0 | 答题界面：指引、示例、进度条、自动保存、断点续答 |
| P0 | 个人进度与报酬明细页 |
| P1 | 注意力检查题（防乱答）与作答质量自动校验 |
| P1 | 申诉入口与处理流 |
| P2 | 多语言支持 |

### 5.5 成功指标

- 邀请→完成首个任务的转化率 ≥ 60%；
- 答题中途放弃率 ≤ 15%；
- 报酬相关客诉/申诉率 ≤ 2%。

### 5.6 当前痛点

- 问卷工具东拼西凑，体验差、无断点续答；
- 报酬结算靠人工转账，周期不透明，受访者流失严重；
- 同意授权流程不规范，存在合规风险。

---

## 6. 平台管理员

### 6.1 角色画像

- 负责平台整体的运行：账号与权限、模型凭据、资源与预算、审计与合规；
- 服务多个业务团队（多租户），要做到**隔离、可控、可审计**；
- 对安全事故和成本超支直接负责。

### 6.2 核心诉求拆解：「安全、成本、租户和资源治理」

| 子诉求 | 展开说明 | 背后动机 |
|--------|---------|---------|
| ① 成员与角色管理 | 对接企业 SSO；基于角色的权限控制（RBAC），六类角色各看各的、各做各的 | 最小权限原则，降低数据泄露面 |
| ② 多租户隔离 | 团队/项目级数据隔离，跨租户不可见；支持项目归档与交接 | 多业务共用平台的前提 |
| ③ 模型凭据托管 | 各家模型 API Key 统一加密托管、按项目授权使用、支持轮换；使用方看不到明文 | Key 散落在个人手里 = 安全与成本双失控 |
| ④ 预算与配额 | 按租户/项目设调用预算与速率配额，超限告警/熔断；成本按项目分摊出账 | 防止一个失控任务烧掉全月预算 |
| ⑤ 审计日志 | 谁、何时、对什么数据/任务做了什么操作，全量留痕、可检索、防篡改 | 安全事件回溯与外部审计要求 |
| ⑥ 合规配置 | 数据保留期限、匿名化/脱敏策略、受访者同意书模板、数据出境限制等策略化配置 | 把合规要求固化成平台策略而非口头约定 |

### 6.3 典型场景（用户故事）

1. 作为管理员，我想新业务团队入驻时创建独立项目空间、绑定预算和角色，以便 10 分钟完成开通且互不干扰。
2. 作为管理员，我想统一托管 OpenAI/千问等模型的 Key 并按项目授权，以便随时轮换 Key 而业务无感。
3. 作为管理员，我想设置"某项目月预算 5000 元，达到 80% 告警、100% 熔断"，以便成本永不失控。
4. 作为管理员，我想在安全审计时检索"过去 90 天谁导出过标注数据"，以便快速完成取证。

### 6.4 功能需求

| 优先级 | 功能 |
|-------|------|
| P0 | SSO 接入 + RBAC 角色权限体系 |
| P0 | 多租户项目空间与数据隔离 |
| P0 | 模型凭据加密托管、按项目授权、轮换 |
| P0 | 预算/配额管理：告警、熔断、账单分摊 |
| P0 | 全量审计日志（含数据导出行为） |
| P1 | 合规策略中心（保留期、脱敏、同意书模板管理） |
| P2 | 资源用量报表与容量规划视图 |

### 6.5 成功指标

- 权限越权事件为 0，Key 明文暴露事件为 0；
- 项目成本超支率为 0（熔断兜底）；
- 新团队开通耗时 ≤ 10 分钟；审计取证响应 ≤ 1 小时。

### 6.6 当前痛点

- API Key 在各团队手里各自保管，无法轮换、无法核算成本；
- 无统一权限体系，数据可被跨团队随意访问；
- 操作无留痕，出了问题查无可查。

---

## 7. 跨角色公共需求（从各角色诉求中收敛）

1. **统一的评测标准资产**：PM 定义的评分规则 → 标注端的作业指引 → 质量端的门禁口径，必须是同一份可版本化的资产（避免三张皮）。
2. **全链路可追溯**：数据集、模型、规则、报告、验收均有版本与操作记录，支撑复现（工程师）、留档（质量）、审计（管理员）。
3. **通知与工作流引擎**：提测→回归→签收、争议→仲裁→反哺、超限→告警→熔断，都是"事件驱动 + 待办"模式，应由统一工作流支撑。
4. **报表与导出**：每个角色都有"向别人证明"的场景（汇报、结算、审计），报告导出能力是公共基础件。

## 8. 后续动作建议

- [ ] 按角色逐一访谈校准本文档假设（每角色至少 2 人），标记与实际不符的诉求；
- [ ] 将 P0 功能映射成 MVP 范围，先跑通「PM 自助 PoC → 工程师评测 → 质量门禁」主链路；
- [ ] 标注/仲裁与外部受访者模块可作为二期，先用现有工具过渡；
- [ ] 评分标准可直接复用仓库中《图片描述质量度量方案》作为第一个落地的评测模板。
