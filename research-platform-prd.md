# 算法效果调研平台 产品需求文档(PRD)

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 算法效果调研平台 PRD |
| 版本 | V1.3 |
| 状态 | 评审稿 |
| 撰写日期 | 2026-07-21 |
| 最近更新 | 2026-07-22 |
| 目标读者 | 产品、研发、算法、测试、运营 |

**修订记录**

| 版本 | 日期 | 修订内容 |
| --- | --- | --- |
| V1.0 | 2026-07-21 | 初稿 |
| V1.1 | 2026-07-22 | 依据业务确认更新:明确目标用户(运营定向邀请约 200 人,覆盖英/法/德/日);「高光时刻」明确包含照片与视频本体,补充视频播放与带宽相关需求;用户激励方案标记为保留项;每题打分人数采用平台默认配置;原"待确认事项"更新为"已确认事项与保留项" |
| V1.2 | 2026-07-22 | 一期范围调整(以下均为一期必交付,无二期规划):① 新增「关键词搜索」算法接入与打分流程;② 多语言全覆盖(英/法/德/日),界面按用户 IP 自动定位语言并支持手动切换;③ 用户激励方案由保留项改为一期上线:积分体系 + 积分兑换周边奖励 |
| V1.3 | 2026-07-22 | 管理后台纳入一期交付并展开功能定义(新增 5.10):管理员账号体系(默认管理员首次登录成为超级管理员,可创建新账号)、模型数据管理、统计看板、用户名单与批量邮件邀请、邀请码管理、兑换审核、审计日志;同步更新范围/里程碑/确认事项 |

---

## 1. 背景与目标

### 1.1 项目背景

公司算法中台持续产出多种视觉算法能力。其中「高光时刻」(Highlight Moments,从用户相册/视频中挑选精彩瞬间)、「美学评分」(Aesthetic Scoring,对图片美学质量打分)与「关键词搜索」(Keyword Search,按关键词在图库中检索相关图片)三个模型已进入效果验证阶段,主要面向海外市场。

目前模型效果的评估依赖内部小规模人工评测,存在两个突出问题:

1. **评估样本量小、评估人群单一**:内部同学的审美偏好与海外目标用户存在差异,评测结论不能代表真实用户感受;
2. **缺少可回流的主观标注数据**:模型迭代缺少大规模、带主观评价的训练数据,难以做偏好对齐(preference alignment)类的再训练。

因此需要搭建一个**面向海外真实用户的调研平台**:算法中台将图片等数据推送至平台,平台组织海外用户对模型输出效果进行多维度主观打分,打分结果一方面用于**评估模型效果**,另一方面回流作为**训练数据**用于模型再训练。

### 1.2 项目目标

| 目标 | 说明 | 衡量方式 |
| --- | --- | --- |
| 建立模型效果的用户侧评估通道 | 让海外真实用户对「高光时刻」「美学评分」「关键词搜索」输出打分 | 每个模型版本可产出评估报告 |
| 沉淀主观标注训练数据 | 打分数据结构化落库,可导出供算法再训练 | 可按模型/版本/维度导出标准格式数据集 |
| 控制参与门槛与数据质量 | 简单登录 + 防恶意分享 + 打分质控 | 无效打分占比、异常账号占比可监控 |
| 平台可扩展 | 一期接入三个算法,架构上支持后续接入更多算法 | 新算法接入不改动平台核心流程 |

### 1.3 一期范围(In Scope)

一期为完整交付版本,以下全部在一期内完成,**无二期规划**:

- 接收算法中台推送的「高光时刻」「美学评分」「关键词搜索」三类评测数据(高光时刻含**照片与视频本体**);
- 海外用户简单登录(邮箱验证码 / Google 账号登录);
- 邀请制访问 + 防恶意分享机制;
- 三类算法各自的多维度主观打分流程;
- 打分数据统计看板与数据导出(评估用途 + 训练用途两种格式);
- 基础的数据质控(注意力检测、异常账号识别);
- **多语言全覆盖**:界面支持英语/法语/德语/日语四种语言,按用户当地 IP 自动定位默认语言,并支持手动切换(详见 5.9);
- **用户激励**:积分体系一期上线,有效打分累计积分,积分达标可**兑换周边奖励**(详见 5.8);
- **管理后台**:管理员账号登录后使用;默认管理员账号首次登录即成为**超级管理员**,可创建新的管理员账号;功能覆盖邀请码生成与管理、模型数据管理、统计看板、用户名单管理与**批量邮件邀请**(按名单发送邮件 + 专属邀请码)、兑换审核与审计日志(详见 5.10)。

### 1.4 一期不做(Out of Scope)

- 除上述三个算法外的其他算法接入(架构预留,不做界面与流程);
- 平台向算法中台的实时回传(一期以离线导出为主,回传接口后续按需建设)。

### 1.5 目标用户(已确认)

- **招募方式**:由**运营团队定向邀请**,不做公开招募;
- **规模**:一期约 **200 人**;
- **地区分布**:**英国、法国、德国、日本**;
- 产品影响:① 用户量级小、可控,风控以"防外流扩散"为主而非高并发对抗;② 英/法/德属 GDPR 辖区、日本适用 APPI,隐私合规要求保留;③ **界面根据当地 IP 自动定位语言**(英/法/德/日),用户可手动切换并记住偏好,打分文案按语言本地化(短句 + 分值锚点降低理解成本)。

---

## 2. 名词解释

| 名词 | 说明 |
| --- | --- |
| 算法中台 | 公司内部算法服务平台,负责产出模型推理结果并推送评测数据到本平台 |
| 高光时刻(Highlight) | 从一组照片/视频帧中选出"精彩瞬间"的算法,输出为选中的图片(可含视频片段封面)及排序 |
| 美学评分(Aesthetic) | 对单张图片的美学质量输出分数(如 0–100)的算法 |
| 关键词搜索(Keyword Search) | 根据文本关键词在图库中检索并按相关性排序返回图片的算法,输出为检索结果图片列表及排序 |
| 评测任务(Task) | 中台一次推送的数据经平台组织后形成的、可分配给用户打分的最小工作单元 |
| 评测题目(Item) | 任务中的一道题,对应一组素材 + 一套打分维度 |
| 打分(Rating) | 用户对一道题目在各维度上提交的主观分数与可选反馈 |
| 金标题(Golden Item) | 答案明确、用于检测用户是否认真作答的质控题 |
| 邀请码(Invite Code) | 控制平台准入的凭证,绑定发放批次,可控制使用次数与有效期 |

---

## 3. 用户角色与场景

### 3.1 角色定义

| 角色 | 说明 | 主要操作 |
| --- | --- | --- |
| 海外评测用户(C 端) | 由运营团队定向邀请的海外真实用户,一期约 200 人,分布于英/法/德/日 | 登录、领取任务、打分、查看自己的参与记录 |
| 运营/项目管理员(B 端) | 内部运营与项目负责人 | 管理邀请码、管理模型数据与任务上下线、查看看板、维护用户名单并批量发送邮件邀请、兑换审核、处理异常账号 |
| 超级管理员(B 端) | 首个登录默认管理员账号的人 | 具备运营管理员全部权限,另可创建/停用管理员账号、重置密码、查看审计日志 |
| 算法/数据同学(B 端) | 算法中台侧的使用方 | 通过 API 推送数据、下载评估报告与训练数据集 |
| 系统管理员 | 平台维护者 | 账号权限管理、系统配置 |

### 3.2 核心用户故事

1. 作为**算法同学**,我把某版本「高光时刻」模型在测试集上的输出通过 API 推送到平台,平台自动生成评测任务,我不需要手工整理问卷。
2. 作为**海外用户**,我收到邀请链接,用邮箱或 Google 账号简单登录后,即可开始对图片效果打分,每道题 30 秒内可完成。
3. 作为**运营**,我发现某个邀请码被发到了公开论坛,我可以一键作废该邀请码,并冻结由它注册的可疑账号。
4. 作为**算法同学**,评测结束后我导出两份数据:一份是按维度聚合的模型效果报告,一份是逐条打分明细(JSONL),直接用于偏好训练。

---

## 4. 整体产品架构与流程

### 4.1 系统架构(逻辑视图)

```
┌──────────────┐   push(API)   ┌─────────────────────────────────────┐
│   算法中台    │ ────────────▶ │              调研平台                │
│              │               │                                     │
│ · 高光时刻    │               │  ┌─────────┐  ┌─────────┐  ┌──────┐ │
│ · 美学评分    │ ◀──────────── │  │数据接入  │  │任务/题目 │  │打分  │ │
│ · 关键词搜索  │  数据集导出/   │  │ 服务    │→│ 管理     │→│ 服务 │ │
└──────────────┘  报告下载      │  └─────────┘  └─────────┘  └──────┘ │
                               │  ┌─────────┐  ┌─────────┐  ┌──────┐ │
      ┌────────┐               │  │账号/登录 │  │防滥用/  │  │统计/ │ │
      │海外用户 │ ────────────▶ │  │ 服务    │  │ 风控    │  │导出  │ │
      └────────┘   Web(H5/PC)  │  └─────────┘  └─────────┘  └──────┘ │
                               └─────────────────────────────────────┘
```

### 4.2 端到端主流程

```
中台推送数据 → 平台校验/落库 → 生成评测任务与题目
    → 运营配置任务(维度模板、每题目标打分人数、上线时间)并上线
    → 用户通过邀请链接进入 → 简单登录 → 领取题目 → 逐题多维度打分
    → 平台质控(金标题/行为检测) → 有效数据入库
    → 统计看板实时更新 → 达到目标样本量后任务自动/手动结束
    → 导出评估报告 + 训练数据集
```

---

## 5. 功能需求详述

## 5.1 数据接入模块(对接算法中台)

### 5.1.1 推送方式

- 中台通过 **HTTPS API(服务端到服务端)** 向平台推送评测数据;鉴权采用 AppKey/AppSecret + 签名,支持 IP 白名单。
- 图片、视频等大文件不直接走接口体,由中台先上传至双方约定的对象存储(海外可访问的 CDN 加速桶),接口中只传**资源 URL + 校验信息(md5/尺寸/视频时长)**。
- 支持批量推送与断点补推;同一 `batch_id` 内按 `item_id` 幂等,重复推送覆盖旧数据(任务未上线时)或拒绝(任务已上线)。

### 5.1.2 推送数据结构(约定)

通用批次字段:

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| batch_id | string | 是 | 中台批次唯一 ID,幂等键 |
| algo_type | enum | 是 | `highlight` / `aesthetic` / `keyword_search`(一期这三个,枚举可扩展) |
| model_version | string | 是 | 模型版本号,用于按版本对比效果 |
| items | array | 是 | 题目素材列表 |
| callback_url | string | 否 | 预留:任务完成后的结果回调地址(回传接口后续按需建设) |

`highlight` 类 item:

| 字段 | 说明 |
| --- | --- |
| item_id | 题目唯一 ID |
| source_set | 原始素材组 URL 列表,每个元素含 `media_type`(photo/video)、URL、视频封面帧 URL、视频时长,用于展示"从哪些素材中选出" |
| highlight_result | 模型选出的高光内容列表(**照片或视频片段本体**),每个元素含 `media_type`、资源 URL、视频封面帧 URL、视频时长、排序、模型置信度 |
| meta | 拍摄场景标签、时间跨度等可选信息 |

> 视频素材约定:视频片段本体以 MP4(H.264)交付,单片段建议 ≤ 30s、≤ 50MB;中台推送前需转码完成,平台不做二次转码;封面帧由中台一并提供,缺失时平台取首帧兜底。

`aesthetic` 类 item:

| 字段 | 说明 |
| --- | --- |
| item_id | 题目唯一 ID |
| image_url | 待评图片 |
| model_score | 模型给出的美学分(用户不可见,仅用于事后与人评对比) |
| meta | 图片类目(人像/风景/食物…)等可选信息 |

`keyword_search` 类 item:

| 字段 | 说明 |
| --- | --- |
| item_id | 题目唯一 ID |
| query | 搜索关键词文本,附 `query_lang`(en/fr/de/ja),题目按对应语言用户优先分发 |
| search_results | 模型返回的结果图片列表(URL、排序、模型相关性分;相关性分用户不可见) |
| gallery_ref | 可选:被检索图库的抽样参考图集(用于用户判断"是否有明显相关图未被搜出") |
| meta | 图库规模、关键词类目(物体/场景/人物/事件…)等可选信息 |

> 关键词搜索约定:单题结果图建议 ≤ 20 张(超出的截断至 Top 20 并在推送回执中说明);query 需与目标用户语言匹配——法/德/日语关键词题目仅分发给对应语言的用户,避免用户看不懂关键词导致打分失真。

### 5.1.3 校验与落库

- 平台接收后进行:字段完整性校验、资源可达性抽检、图片合规性机审(涉黄涉暴过滤,不合规素材标记剔除并在回执中说明);
- 校验通过的批次进入「待配置」状态,由运营在管理后台配置为正式评测任务;
- 向中台返回批次接收回执:成功条数、失败条数及原因明细。

### 5.1.4 验收要点

- [ ] 同一批次重复推送不产生重复题目;
- [ ] 单批次 10,000 items 推送在 10 分钟内完成落库与校验;
- [ ] 不合规素材不会出现在任何用户可见界面。

## 5.2 任务与题目管理(管理后台)

- **任务生成**:每个 `batch_id` 默认生成一个评测任务;运营可将多个批次合并为一个任务(仅限同一 algo_type)。
- **任务配置项**:
  - 打分维度模板(见 5.5,提供默认模板,可增删维度、改文案);
  - 每道题目标有效打分人数 N(默认 5,可配 3–20;算法侧暂无统计功效方面的硬性要求,一期采用平台默认值,后续如有要求再按任务调整);
  - 每用户每日可打分题目上限(默认 50);
  - 金标题插入比例(默认 5%);
  - 任务上线/下线时间、目标用户分组(按邀请码批次圈定)。
- **任务状态机**:`待配置 → 待上线 → 进行中 → 已暂停 → 已完成/已下线`;
- **题目分发策略**:
  - 平台自动将题目分发给在线用户,优先分发"已有打分数最少"的题目,保证样本均匀;
  - 同一用户不会重复领到同一题目;
  - 单题达到目标 N 个有效打分后停止分发。

## 5.3 用户登录与账号(简单登录)

设计原则:**门槛尽量低(海外用户、非专业标注员),但必须可追溯到唯一真实账号**(打分数据要可信、可清洗)。

### 5.3.1 登录方式

| 方式 | 说明 | 优先级 |
| --- | --- | --- |
| Google OAuth | 海外用户覆盖率最高,一键登录 | P0 |
| 邮箱 + 验证码 | 无 Google 账号用户的兜底;仅验证码,不设密码 | P0 |
| Apple 登录 | iOS 用户体验好;若一期仅 Web 可降为 P1 | P1 |

- 首次登录即注册,自动创建平台账号(`user_id`);
- 首次登录需勾选**用户协议与隐私政策**(GDPR 合规,明确告知打分数据将用于模型评估与训练);
- 登录态:Web 端 Session/JWT,有效期 7 天,支持静默续期;
- 不采集非必要个人信息;仅保存邮箱(或第三方 OAuth 的 open_id)、昵称、地区、语言。

### 5.3.2 用户端账号相关页面

- 登录页(邀请链接落地页,见 5.4);
- 个人中心:我的打分记录(仅条数与日期,不展示题目内容)、我的积分与兑换入口(见 5.8)、语言切换(见 5.9)、账号注销入口(GDPR 要求,注销后个人信息删除、打分数据匿名化保留)。

## 5.4 准入控制与防恶意分享(风控)

**风险定义**:邀请链接/邀请码被参与者转发到公开渠道(社群、论坛、羊毛党渠道),导致:① 非目标人群涌入,数据分布失真;② 刷分/机器批量作答,污染训练数据;③ 评测素材(内部测试图片)被扩散。

### 5.4.1 准入机制(邀请制)

- 平台**不开放公开注册**,必须持有效邀请凭证才能完成首次登录;
- 邀请凭证由**运营团队**在后台生成并通过定向渠道(邮件/社群私信)一对一发放给受邀用户(一期约 200 人);
- 邀请凭证两种形态:
  - **邀请码**:运营在后台按批次生成,可配置——最大使用次数(默认 1 码 1 人)、有效期、绑定任务分组;
  - **专属邀请链接**:邀请码的链接化形态,带一次性 token;
- 每个账号记录其来源邀请码批次,用于事后按渠道分析数据质量与追责。

### 5.4.2 防分享/防滥用手段

| 层级 | 手段 | 说明 |
| --- | --- | --- |
| 凭证层 | 一次性邀请码 | 默认一码一用,用后即失效;多次使用的码可随时作废 |
| 凭证层 | 链接 token 时效 | 邀请链接 token 默认 72 小时有效,过期需重新发放 |
| 账号层 | 设备指纹 | 采集浏览器指纹,同一设备注册账号数超阈值(默认 3)则拦截并进人工审核 |
| 账号层 | 频控 | 同 IP 段单日新注册数、单账号打分速率(如平均每题 < 3 秒判为异常)限制 |
| 账号层 | 验证码挑战 | 注册与异常行为时触发 CAPTCHA(海外可用的 reCAPTCHA/hCaptcha) |
| 内容层 | 素材防扩散 | 评测图片加动态水印(打分者 user_id 哈希暗纹);视频经播放器叠加半透明水印层展示;禁右键/长按保存(前端约束);图片与视频 URL 均为短时效签名 URL |
| 内容层 | 禁止分享题目页 | 题目页 URL 不含可枚举 ID,且校验登录态 + 领题记录,无法通过转发链接直接查看 |
| 数据层 | 异常账号处置 | 后台支持冻结账号、批量作废其打分数据、作废关联邀请码 |

### 5.4.3 验收要点

- [ ] 未持有效邀请凭证无法进入任何评测页面;
- [ ] 已使用的一次性邀请码再次使用时给出明确失败提示;
- [ ] 冻结账号后,其历史打分在导出数据中默认被排除(可选包含并带标记)。

## 5.5 打分模块(核心功能)

### 5.5.1 通用交互流程

```
进入任务 → 阅读一次性说明页(打分标准示例) → 系统下发题目
  → 展示素材 → 用户逐维度打分(+可选文字反馈) → 提交
  → 自动下发下一题 → 可随时退出,进度保留
```

通用要求:

- 移动端 Web 优先(海外用户以手机为主),PC 自适应;
- 界面与打分文案支持英/法/德/日四种语言,按用户当前语言展示(本文档维度表中的打分问题以英文示意,四语文案见多语言模块 5.9);
- 每道题所有必填维度打完才能提交;支持"跳过"(记录跳过原因:看不懂/图片加载失败/其他);
- 打分控件默认 **5 分李克特量表**(1=Very poor … 5=Excellent),每档有文字锚点说明,降低理解成本;
- 记录每题作答时长、维度修改次数等行为数据(供质控);
- 图片加载失败自动重试并可上报,该次作答不计入该用户跳过率。

### 5.5.2 「高光时刻」打分设计

**展示方式**:题目页先平铺展示原始素材组(缩略图网格,视频素材缩略图带时长角标与播放图标,可点开大图/播放),再突出展示模型选出的高光结果(照片或视频片段,选中项带高亮标识与顺序)。

**视频播放要求**(高光结果包含视频片段本体):

- 内置轻量播放器(HTML5 Video),支持点击播放/暂停、进度条、静音切换;默认静音、点击后播放,不自动连播;
- 视频加载采用封面帧占位 + 按需加载,列表中不预载视频本体;
- 播放失败自动重试一次,仍失败提供"上报问题"入口,该题可跳过且不计入用户跳过率;
- 打分前置校验:高光结果中的视频至少被播放过一次(播放进度 ≥ 50% 或播完)才允许提交,防止未观看即打分。

**默认打分维度**(可在后台模板中调整):

| 维度 | 打分问题(英文界面示意) | 量表 |
| --- | --- | --- |
| 精彩程度 Highlight Quality | Do the selected moments feel like true "highlights"? | 1–5 |
| 选取准确性 Selection Accuracy | Did the model pick the best photos from this set? | 1–5 |
| 覆盖完整性 Coverage | Are any great moments missing from the selection? | 1–5(5=nothing missing) |
| 排序合理性 Ordering | Is the order of highlights reasonable? | 1–5 |
| 整体满意度 Overall | Overall, how satisfied are you with this highlight reel? | 1–5 |

**补充交互**:

- 允许用户在原始素材中**点选"我认为更应该入选"的照片/视频**(多选,可跳过)——该数据是再训练最直接的偏好信号;
- 可选文字反馈框(200 字符以内)。

### 5.5.3 「美学评分」打分设计

**展示方式**:单图大图展示,支持双击放大;**不向用户展示模型分数**,避免锚定效应。

**默认打分维度**:

| 维度 | 打分问题(英文界面示意) | 量表 |
| --- | --- | --- |
| 构图 Composition | How good is the composition/framing? | 1–5 |
| 色彩与光影 Color & Lighting | How pleasing are the colors and lighting? | 1–5 |
| 清晰度 Clarity | How is the sharpness/technical quality? | 1–5 |
| 内容吸引力 Content Appeal | How interesting/appealing is the subject? | 1–5 |
| 整体美感 Overall Aesthetic | Overall, how beautiful is this photo? | 1–5 |

**效果评估口径**:平台侧将用户「整体美感」均分与模型 `model_score` 做相关性分析(Spearman/Pearson),作为美学模型效果核心指标(见 5.7)。

### 5.5.4 「关键词搜索」打分设计

**展示方式**:题目页顶部醒目展示搜索关键词(按用户语言分发,见 5.1.2),下方按模型返回顺序网格展示结果图(带序号,可点开大图);如有 `gallery_ref`,提供"查看图库参考"折叠入口。**不向用户展示模型相关性分**,避免锚定效应。

**默认打分维度**(可在后台模板中调整):

| 维度 | 打分问题(英文界面示意) | 量表 |
| --- | --- | --- |
| 结果相关性 Relevance | How well do the results match the keyword? | 1–5 |
| 头部准确性 Top Results | Are the first few results the most relevant ones? | 1–5 |
| 排序合理性 Ranking | Is the ranking from most to least relevant reasonable? | 1–5 |
| 结果纯净度 Precision | How many results are clearly irrelevant?(5=none) | 1–5 |
| 整体满意度 Overall | Overall, how satisfied are you with this search result? | 1–5 |

**补充交互**:

- 允许用户对每张结果图做**逐图相关性标记**(三档:Relevant / Partially relevant / Not relevant,可跳过)——逐图标记是搜索模型再训练(相关性排序学习)最直接的监督信号;
- 如提供 `gallery_ref`,允许用户点选"图库中明显相关但未被搜出"的图片(多选,可跳过),用于评估召回;
- 可选文字反馈框(200 字符以内)。

**效果评估口径**:以用户逐图相关性标记为真值,计算模型排序的 nDCG / Precision@K,并与维度均分共同构成搜索模型效果核心指标(见 5.6/5.7)。

### 5.5.5 数据质控

- **金标题**:每约 20 题插入 1 道答案明确的质控题(如明显糊片 vs 清晰佳片);金标题连续答错 2 次,当日打分标记为低置信,答错率 > 40% 的账号进入人工审核;
- **行为检测**:作答时长过短(< 3s/题)、全程同一分值(直线作答)、维度间零方差等模式自动标记;
- **有效性标记**:每条打分记录带 `validity` 字段(valid / suspicious / invalid),统计与导出时可按有效性过滤;
- **一致性度量**:平台按题目计算标注者间一致性(如 Krippendorff's α),供算法侧判断维度设计是否可靠。

## 5.6 统计看板(管理后台)

- **任务进度**:总题数、已完成题数(达到 N 个有效打分)、参与人数、日打分量趋势;
- **模型效果概览**:
  - 各维度均分、分布直方图(按 algo_type / model_version / 题目 meta 标签下钻);
  - 版本对比:同一测试集在不同 `model_version` 下的维度分对比;
  - 美学专属:人评分 vs 模型分散点图与相关系数;
  - 高光专属:用户"更应入选"点选与模型选择的重合率;
  - 搜索专属:以用户逐图相关性标记为真值的 nDCG / Precision@K、按关键词语言与类目下钻的相关性分布、"漏搜"点选率;
- **数据质量**:有效打分率、金标题通过率、异常账号数、各邀请渠道数据质量对比;
- 图表支持导出 PNG/CSV。

## 5.7 数据导出(评估 + 训练两种用途)

### 5.7.1 评估报告导出

- 面向:算法、产品、管理层;
- 形式:后台一键导出 CSV/Excel + 概览 PDF;
- 内容:任务基本信息、样本量与有效率、各维度统计值(均值/中位数/标准差/分布)、版本对比结论、一致性指标。

### 5.7.2 训练数据集导出

- 面向:算法再训练;
- 形式:JSONL(一行一条打分明细)+ 数据集说明文件;
- 明细字段:`item_id, algo_type, model_version, media_refs(资源URL), model_output(模型选择/模型分/搜索结果排序), user_id(脱敏哈希), user_lang, ratings{维度:分值}, user_selection(高光题"更应入选"点选/搜索题"漏搜"点选), result_marks(搜索题逐图相关性标记), free_text, duration_ms, validity, rated_at`;
- 支持按任务、时间范围、有效性、维度筛选导出;
- 导出操作需管理员权限并留审计日志;
- 后续按需:通过 API 供中台直接拉取(与 5.1 的 callback 配套)。

## 5.8 积分与激励(一期上线)

**目标**:以积分 + 周边奖励提升约 200 名志愿用户的参与度与留存,同时不引入"为刷分而乱答"的负向激励。

### 5.8.1 积分规则

| 行为 | 积分 | 说明 |
| --- | --- | --- |
| 完成一道有效打分 | +10 | 仅 `validity = valid` 的打分计分;suspicious 暂缓入账,复核通过后补发 |
| 当日完成 20 道有效打分 | +50(日上限 1 次) | 鼓励持续参与 |
| 金标题连续 10 题全对 | +30 | 奖励认真作答,对冲"快答刷分"倾向 |
| 打分被判定 invalid / 账号被冻结 | 该部分积分不入账 / 已入账积分冻结 | 风控联动(见 5.4.2) |

- 积分只增不减(除风控冻结),明细可在个人中心查询(行为、时间、积分变动);
- 积分规则文案在四种语言下同步展示。

### 5.8.2 周边兑换

- 个人中心提供**兑换页**:展示可兑换周边列表(由运营在后台配置:名称、图片、所需积分、库存、四语描述);
- 用户积分达标即可提交兑换申请,填写收货信息(仅用于寄送,单独存储、寄送完成后按期删除);
- 兑换流程:用户申请 → 积分预扣 → 运营后台审核(校验账号有效性)→ 发货并回填物流单号 → 用户侧展示兑换状态(申请中/已发货/已完成/已驳回,驳回则积分返还);
- 后台支持:奖品与库存管理、兑换审核、发货登记、兑换记录导出;
- 防刷约束:兑换前账号需通过风控校验(无未处理的异常标记);同一账号兑换频次限制由运营配置。

### 5.8.3 验收要点

- [ ] invalid 打分不产生积分;账号冻结后其积分同步冻结、无法兑换;
- [ ] 兑换驳回后积分自动返还且有明细记录;
- [ ] 收货信息与打分数据分库存储,导出训练数据中不包含任何收货信息。

## 5.9 多语言(一期全覆盖)

**范围**:平台所有用户可见界面与文案——登录/落地页、说明页、打分页(维度问题、分值锚点、按钮、提示)、个人中心、积分与兑换、邮件通知、协议文本——一期全部支持**英语、法语、德语、日语**四种语言。

### 5.9.1 语言定位与切换

- **默认语言**:首次访问按用户 **IP 归属地自动定位**(英国→EN,法国→FR,德国→DE,日本→JA;其他地区兜底 EN);
- **手动切换**:全局导航提供语言切换器,切换后立即生效并写入用户偏好(登录用户存服务端,未登录存本地),后续访问以**用户偏好优先于 IP 定位**;
- 语言偏好同时用于题目分发:关键词搜索题目按 `query_lang` 与用户语言匹配分发(见 5.1.2)。

### 5.9.2 文案与实现要求

- 全部文案走 i18n 配置(key-value 多语言资源),禁止硬编码;新增文案缺译时兜底英文并在后台标红提示;
- 打分维度问题与分值锚点属于**评测措辞**,四语版本需经母语者/专业翻译审校,保证跨语言语义一致(否则不同地区打分不可比),上线前冻结措辞版本并纳入版本管理;
- 日期、数字格式随语言本地化;德语/法语长词需在移动端做断行与控件宽度适配;
- 用户文字反馈按原语言存储并标记语言,导出时附 `lang` 字段。

### 5.9.3 验收要点

- [ ] 四语言下所有页面无缺译、无布局破版;
- [ ] IP 定位与手动切换的优先级正确(偏好 > IP),偏好在重新登录后保持;
- [ ] 同一道题在四种语言下维度语义一致(抽样审校通过)。

## 5.10 管理后台(一期上线)

管理后台是运营与算法同学的统一工作台,与 C 端同域部署、独立入口(`/admin`),所有页面需管理员登录后访问。核心三块:**模型数据管理、统计看板、用户名单邮件邀请**,并配套账号体系与运营辅助功能。

### 5.10.1 管理员账号与权限

- **默认管理员账号**:系统初始化内置一个默认管理员(邮箱 `111@qq.com`,初始密码 `111`,示例配置——生产环境通过环境配置下发,并在**首次登录时强制修改密码**);
- **超级管理员**:默认账号首次登录后即成为超级管理员(全局唯一角色);
- **账号管理**(仅超级管理员可见):
  - 创建管理员账号:填写邮箱 + 初始密码,新账号首次登录强制改密;
  - 停用/启用账号、重置密码;超级管理员不可被停用,不可停用自己;
- **角色权限矩阵**:

| 功能 | 超级管理员 | 运营管理员 |
| --- | --- | --- |
| 邀请码/模型数据/看板/名单邮件/兑换审核 | ✓ | ✓ |
| 训练数据导出(含打分明细) | ✓ | ✓(操作留审计日志) |
| 账号管理(创建/停用/重置密码) | ✓ | ✗ |
| 审计日志查看 | ✓ | ✗ |

- **安全要求**:登录鉴权在服务端完成(密码加盐哈希存储,禁止明文);登录态有效期 24h,连续输错 5 次锁定 15 分钟;建议接入公司统一 SSO/双因素认证(如具备条件)。

### 5.10.2 模型数据管理

- **批次列表**:展示中台推送的全部批次(批次号、算法类型、模型版本、题目数、推送时间、状态、已收集有效打分数);
- **批次操作**:上线/下线(下线后立即停止向用户分发)、合并为任务(同 5.2)、查看推送回执(失败明细);
- **题目明细**:进入批次可查看每道题——素材预览(图片缩略图/视频封面)、打分进度(已收集 n/N)、各维度均分、跳过率;支持人工**剔除不合规/有问题的题目**(剔除后不再分发,已收集数据标记);
- **数据导出**:入口集中在此模块——评估报告(CSV/Excel)与训练数据集(JSONL),口径见 5.7;导出留审计日志。

### 5.10.3 统计看板

看板指标口径详见 5.6(任务进度、模型效果概览、数据质量),管理后台首页即看板。在 5.6 基础上补充运营视角:

- **邀请漏斗**:名单人数 → 邮件送达 → 注册 → 首次打分 → 活跃(近 7 日),按邀请批次/地区下钻;
- **积分与兑换**:积分发放总量、兑换申请数/完成数、库存水位;
- **今日速览卡片**:今日打分量、新增注册、待处理兑换申请、待审核异常账号。

### 5.10.4 用户名单与批量邮件邀请

- **名单导入**:支持粘贴文本(换行/逗号分隔)与 CSV 上传;自动做邮箱格式校验、去重(与已有名单合并);每条记录含:邮箱、地区(英/法/德/日)、语言(默认随地区)、备注;
- **名单管理**:列表查看(邮箱、地区、分配的邀请码、发送状态、注册状态)、编辑地区/语言、移除;名单与注册账号自动关联(邮箱匹配),形成"受邀→注册→打分"追踪;
- **批量邮件邀请**:
  - 勾选名单成员(支持"全选未发送")→ 发送邀请邮件;
  - 系统为每人自动生成**一次性专属邀请码**并写入邮件(码与邮箱绑定,见 5.4.1);
  - 邮件模板多语言:按成员语言选用对应模板(英/法/德/日),模板由运营在后台维护(主题 + 正文,支持变量:邀请码、落地页链接、称呼);
  - 发送前强制**邮件预览**确认;发送后记录发送日志(时间、人数、操作人);
  - 送达状态跟踪(送达/退信/未知),退信成员标记后可修正邮箱重发;对未注册成员支持**重发提醒**(每人限 2 次,间隔 ≥72h,防打扰);
- **发送通道**:接入海外可用的事务邮件服务(如 SES/SendGrid,由基建选型),发送频率遵守服务商限制与反垃圾邮件规范(SPF/DKIM 配置)。

### 5.10.5 邀请码管理

- 批量生成(数量、前缀、每码可用次数、有效期)、作废、复制未用码;
- 列表展示使用情况(已用/上限)、状态(可用/已用完/已作废/已过期)、绑定邮箱、创建时间与操作人;
- 与名单联动:名单邮件发送自动生成并绑定;手工生成的码可导出交由线下渠道发放。

### 5.10.6 兑换审核

积分兑换流程的后台侧(用户侧见 5.8.2):兑换申请列表 → 审核(校验账号风控状态)→ 通过/驳回(驳回退积分)→ 发货登记(物流单号)→ 完成;奖品与库存管理、兑换记录导出。

### 5.10.7 审计日志

关键操作全部留痕(操作人、时间、对象、动作):账号管理、批次上下线、题目剔除、数据导出、邀请码作废、批量邮件发送、兑换审核。仅超级管理员可查看。

### 5.10.8 验收要点

- [ ] 默认管理员首次登录成为超级管理员并被强制改密;新建账号首次登录强制改密;
- [ ] 运营管理员看不到账号管理与审计日志入口;
- [ ] 名单批量发送后,每位成员获得互不相同的一次性邀请码,且码只能被对应成员使用一次;
- [ ] 批次下线后 C 端不再分发该批次题目;
- [ ] 所有导出与敏感操作可在审计日志中查到。

---

## 6. 非功能需求

| 类别 | 要求 |
| --- | --- |
| 部署与访问 | 服务与存储部署在海外可用区;图片与视频均走海外 CDN,CDN 节点需覆盖英/法/德/日四个目标地区;国内办公网可访问管理后台 |
| 性能 | 题目页图片首屏加载 P90 < 2s、视频起播(点击到首帧)P90 < 2s(目标地区);打分提交接口 P99 < 500ms |
| 容量与带宽 | 一期实际用户约 200 人,系统按注册用户 5,000、日打分 2 万条设计余量;素材存量 500 万级;视频按单片段 ≤ 30s/≤ 50MB 估算 CDN 带宽与存储成本,上线前完成带宽压测 |
| 可用性 | 核心链路(登录、领题、提交)可用性 99.9% |
| 合规 | 目标用户覆盖英/法/德(GDPR/UK GDPR)与日本(APPI):隐私政策、数据用途告知、账号注销与数据删除;评测素材需中台确认已获数据使用授权;一期暂不安排专项法务评审,采用公司标准隐私政策模板文案 |
| 安全 | 全站 HTTPS;图片/视频资源签名 URL;后台操作审计日志;打分数据与个人身份数据分库/脱敏关联;管理员登录服务端鉴权(密码加盐哈希、首次登录强制改密、连续输错锁定,见 5.10.1),默认管理员密码上线前必须修改 |
| 国际化 | 一期即支持英/法/德/日四语全覆盖;IP 自动定位默认语言 + 手动切换(偏好优先);文案全部走 i18n 配置,禁止硬编码(详见 5.9) |
| 可扩展 | algo_type 枚举化;打分维度模板化;media_type 支持 photo/video 并可扩展;新增算法仅需新增"展示组件 + 维度模板" |

---

## 7. 埋点与业务指标

**平台运营指标**:邀请转化率(受邀→注册)、注册→首次打分转化率、人均日打分量、7 日留存、积分兑换参与率(有兑换行为的用户占比)、各语言用户活跃分布;

**数据生产指标**:日有效打分量、有效率、单题达标周期(推送→集满 N 个有效打分的时长);

**风控指标**:异常账号占比、邀请码滥用拦截次数、单渠道数据作废率。

关键埋点:落地页曝光、登录成功、说明页完成、每题下发/提交/跳过、维度交互、导出操作。

---

## 8. 里程碑规划(建议)

| 阶段 | 内容 | 周期(建议) |
| --- | --- | --- |
| M1 | 数据接入 API(三类算法、含视频素材)、任务管理、C 端登录(Google+邮箱)、邀请码准入、**管理后台框架与管理员账号体系(默认账号/超管/账号管理)**、i18n 框架搭建(四语资源结构 + IP 定位/切换) | 4 周 |
| M2 | 三类算法打分流程(含视频播放器组件与播放校验、搜索逐图标记)、金标题质控、**管理后台:模型数据管理 + 统计看板 + 邀请码管理**、积分体系(规则计算 + 明细) | 4 周 |
| M3 | 风控完善(设备指纹/图片与视频水印/频控)、导出(评估+训练格式)、**用户名单与批量邮件邀请(邮件服务接入 + 多语言模板 + 送达跟踪)**、周边兑换(兑换页 + 后台审核发货)、审计日志、四语文案翻译审校与全量走查、视频带宽压测、灰度上线 | 3 周 |
| M4 | 运营通过名单邮件完成约 200 人定向邀请,三类算法首个正式评测项目跑通,产出首份评估报告与训练数据集,复盘迭代 | 2 周 |

> 说明:关键词搜索、四语全覆盖、积分兑换、管理后台均为一期必交付项,总周期约 13 周。

---

## 9. 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 一期仅约 200 名用户,参与度不足或流失,样本集不满 | 评估周期拉长 | 积分 + 周边兑换激励拉动参与;运营定向跟进催办;题目分发优先补齐缺口题 |
| 积分激励诱发"为刷分而快答/乱答" | 数据噪声增大 | 仅有效打分计分;金标题全对加成引导认真作答;invalid 不入账、异常账号积分冻结;监控人均作答时长变化 |
| 四语翻译措辞不一致,导致跨地区打分不可比 | 评估结论失真 | 打分措辞经母语者审校并版本冻结;金标题分语言监控通过率,异常语言复核译文 |
| 视频素材拉高 CDN 带宽成本、弱网地区播放卡顿 | 体验差、跳过率升高 | 中台侧限制片段时长/码率;封面占位按需加载;上线前分地区带宽压测 |
| 周边寄送涉及跨国物流与收货信息合规 | 运营成本高、合规风险 | 收货信息单独存储、寄送后按期删除;物流由运营统一走既有跨境渠道;兑换审核拦截异常账号 |
| 批量邀请邮件进垃圾箱、送达率低 | 招募进度受阻 | 选用信誉良好的事务邮件服务并配置 SPF/DKIM;分批限速发送;送达/退信监控,退信修正邮箱后重发 |
| 管理后台默认密码泄露或弱口令 | 数据与名单泄露 | 首次登录强制改密 + 密码强度校验;登录锁定策略;敏感操作审计;默认密码仅存于环境配置 |
| 主观打分噪声大,维度间区分度低 | 训练数据价值打折 | 金标题 + 一致性指标持续监控;小流量先验证维度设计再放量 |
| 邀请码仍被规模化转卖/脚本注册 | 数据污染 | 多层风控叠加;按渠道回溯批量作废;必要时切换为白名单邮箱邀请 |
| 评测素材含用户隐私或版权风险 | 合规事故 | 接入侧机审 + 中台推送前需确认素材授权;水印与签名 URL 防扩散 |
| 中台推送格式频繁变更 | 联调成本高 | 接口版本化(v1 固化),变更走版本升级而非兼容补丁 |

---

## 10. 已确认事项

历次待确认事项均已与业务侧对齐,结论如下(最近更新:2026-07-22,V1.2):

| # | 事项 | 结论 | 状态 | 文档落点 |
| --- | --- | --- | --- | --- |
| 1 | 用户激励方案 | **一期上线**(V1.2 确认,取代 V1.1 的保留结论):积分体系 + 积分达标兑换周边奖励 | 已确认 | 1.3、5.8、8、9 |
| 2 | 目标用户来源 | 由**运营团队**定向邀请,规模约 **200 人**,地区覆盖**英国、法国、德国、日本** | 已确认 | 1.5、3.1、5.4、8(M4) |
| 3 | 高光时刻媒体形态 | 高光时刻包含精选出的**照片或视频本体**(非仅封面帧),平台支持视频播放,并纳入带宽评估与压测 | 已确认 | 5.1.2、5.5.2、6、8、9 |
| 4 | 每题打分人数 N 与总样本量 | 算法侧暂无统计功效方面的硬性要求,一期采用平台默认值(N=5,可按任务配置) | 已确认 | 5.2 |
| 5 | 隐私协议与素材授权的法务评审 | 一期**不安排专项法务评审**,采用公司标准隐私政策模板文案;素材授权由中台在推送前自行确认 | 已确认 | 6(合规) |
| 6 | 算法接入范围 | 一期接入**三个算法**:高光时刻、美学评分、**关键词搜索**(V1.2 新增) | 已确认 | 1.3、5.1.2、5.5.4 |
| 7 | 多语言 | **一期全覆盖**英/法/德/日四语(V1.2 确认,不做二期规划);按 IP 自动定位默认语言,支持手动切换 | 已确认 | 1.3、1.5、5.9、6 |
| 8 | 管理后台 | **一期上线**(V1.3 确认):默认管理员账号(`111@qq.com`,初始密码见环境配置)首次登录成为超级管理员并可创建新账号;含模型数据管理、统计看板、用户名单批量邮件邀请、邀请码管理、兑换审核、审计日志 | 已确认 | 1.3、5.10、8 |

当前无待确认事项与保留项。

---

## 附录 A:核心数据实体(简)

```
User(user_id, oauth_type, email_hash, region, lang_pref, invite_code_id, status, created_at)
InviteCode(code_id, batch, max_use, used_count, expire_at, status)
Batch(batch_id, algo_type[highlight|aesthetic|keyword_search], model_version,
      item_count, status, pushed_at)
Task(task_id, batch_ids[], algo_type, dimension_template_id, target_n, status, online_at)
Item(item_id, task_id, media_payload(json), model_output(json), query_lang, rated_count, status)
Rating(rating_id, item_id, user_id, user_lang, scores(json), user_selection(json),
       result_marks(json), free_text, duration_ms, validity, created_at)
GoldenItem(item_id, expected_answer(json))
AdminAccount(admin_id, email, password_hash, role[super|ops], status[active|disabled],
             must_change_password, created_by, created_at)
RosterMember(member_id, email, region, lang, invite_code, delivery_status[sent|bounced|unknown],
             registered_user_id?, sent_count, last_sent_at)
EmailSendLog(log_id, operator_admin_id, template_lang, recipients_count, sent_at)
AuditLog(audit_id, admin_id, action, target, created_at)
PointsTransaction(tx_id, user_id, type[earn|freeze|redeem|refund], amount, ref_id, created_at)
RewardItem(reward_id, i18n_name(json), points_cost, stock, status)
Redemption(redemption_id, user_id, reward_id, points_cost, status[applied|approved|
           shipped|done|rejected], tracking_no, created_at)
```
