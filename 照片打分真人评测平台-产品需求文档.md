# 照片打分真人评测平台（网页版）· 产品需求文档（PRD）

> 版本：v2.0
> 日期：2026-07-09
> 状态：待评审
> 产品形态：网页（内部管理端 + 面向用户的分享打分页）
> 打分用户：全球 NAS 用户（免注册，打开链接即可参与）
> 本文示例需求：**照片美学打分**（平台按"需求"配置维度，美学打分为首个落地示例）

---

## 一、背景与定位

### 1.1 我们是谁

我们是**模型能力提供方**：向 NAS 产品/生态输出照片处理与打分类的模型能力，例如照片美学打分模型。

### 1.2 要解决的问题

模型上线前后，我们始终需要回答一个问题：

> **模型给照片打的分，和真实用户的判断到底差多少？差在哪些维度、哪些照片上？**

目前缺少一个标准化的验证手段：

- 内部标注成本高、人数少，且标注员审美不能代表全球 NAS 用户；
- 没有工具能把"模型打分 → 真人打分 → 差距对比 → 结论报告"串成一条自动化链路；
- 每换一个需求（美学打分、清晰度评估等），都要重新搭一套收集与分析流程。

### 1.3 产品定位

一个网页版的**模型打分真人评测平台**，完整链路：

> 内部团队上传**模型处理打分后的照片** → 平台按该需求的**维度集**自动生成**分享页** → 全球 NAS 用户打开链接，对照片按相同维度**真人打分** → **云端对比模型**计算模型分与真人分的差距 → **自动生成对比报告**，供内部团队评估与迭代模型。

一句话：**用真实用户的打分，来给我们的模型打分。**

---

## 二、目标与非目标

### 2.1 产品目标

1. 内部团队可在 10 分钟内完成一次评测任务的创建（上传照片与模型分 → 生成分享页链接）。
2. 打分页对全球用户零门槛：免注册、免安装、移动端可用、多语言。
3. 分享页的打分参与率（打开链接后至少给 1 张照片打分）≥ 40%。
4. 报告能直接回答：**整体偏差多少？哪些维度不一致？哪些照片分歧最大？**——可直接用于模型迭代决策。
5. 平台按"需求"抽象：更换维度集即可支持新的评测需求，无需重新开发。

### 2.2 非目标（本期不做）

- 不做面向 C 端用户的照片管理/上传功能（照片仅由内部团队上传）。
- 不做模型推理服务本身（照片在上传前已由模型处理打分完毕；平台只接收结果）。
- 不做公开社区/照片广场（分享页仅限持有链接者访问）。
- 不做真人打分数据的实时模型训练（本期只做差距分析与报告，数据供算法团队离线使用）。

---

## 三、用户与角色

| 角色 | 是谁 | 使用哪端 | 核心行为 |
|------|------|---------|---------|
| 评测管理员 | 内部产品/算法/运营同学 | 管理端（需登录） | 创建评测任务、上传照片与模型分、配置维度集与分享页、查看报告 |
| 打分用户 | 全球 NAS 用户 | 分享打分页（免注册） | 打开链接，浏览照片，按维度打分 |
| 报告读者 | 内部算法/产品团队 | 管理端 | 阅读对比报告，指导模型迭代 |

**打分用户说明**：通过 NAS 产品内推送、用户社区、邮件等渠道触达；来自不同国家/语言/文化背景，其审美多样性正是评测价值所在。平台需记录打分者的语言/地区（匿名，见 §8），供报告分组分析。

---

## 四、核心概念与总体流程

### 4.1 核心概念

| 概念 | 说明 |
|------|------|
| 需求（Demand） | 一类模型能力的评测场景，如"照片美学打分"。每个需求绑定一个**维度集** |
| 维度集（Dimension Set） | 该需求下的打分维度定义（名称、说明、打分范围），带版本号。模型分、真人分、报告必须使用同一版本 |
| 评测任务（Task） | 一次评测：一批照片 + 对应模型打分 + 一个分享页 + 一份报告 |
| 分享页（Share Page） | 由任务自动生成的免注册打分网页，唯一链接对外分发 |
| 对比报告（Report） | 云端模型对"模型分 vs 真人分"的自动分析结果 |

### 4.2 总体流程

```
【管理端 · 内部团队】
  ① 选择需求（例：照片美学打分，自动带出美学维度集）
  ② 上传模型处理过后的照片 + 每张照片的模型打分（总分 + 各维度分）
  ③ 平台校验数据 → 自动生成分享页 → 获得链接
        │
        │ 链接通过 NAS 产品推送 / 社区 / 邮件分发
        ▼
【分享打分页 · 全球 NAS 用户（免注册）】
  ④ 打开链接 → 看到算法按美学打分输出/排序的照片
  ⑤ 对每张照片：给总分 + 按美学各维度逐项打分（盲评：打完才显示模型分）
        │
        │ 打分数据实时回传
        ▼
【云端 · 自动分析】
  ⑥ 云端对比模型：计算模型打分 vs 用户真实打分的差距
     （整体偏差、维度级偏差、排序一致性、分歧照片挖掘）
  ⑦ 自动生成对比报告 → 通知内部团队
```

---

## 五、示例需求：照片美学打分维度集（v1）

以下为首个需求"照片美学打分"的维度集定义。模型对每张照片输出这些维度的分数，用户在分享页上按**完全相同的维度**打分。

**总分**：0～100 分；**各维度**：0～10 分。

| 维度 | 说明 | 用户侧引导语（打分页展示） |
|------|------|--------------------------|
| 构图 | 主体位置、画面平衡、引导线、裁切 | 主体是否突出？画面是否协调不杂乱？ |
| 色彩 | 色调和谐度、饱和度、白平衡 | 颜色看起来是否舒服、有风格？ |
| 光影 | 曝光、对比度、光线运用 | 明暗是否合适？光影是否有层次？ |
| 清晰度与质感 | 对焦、噪点、动态模糊 | 主体是否清晰？画质是否干净？ |
| 主体与瞬间 | 拍摄对象的表情、姿态、抓拍时机 | 是否抓到了好瞬间？ |
| 氛围与情感 | 画面传达的情绪与故事感 | 这张照片是否打动你？ |

> - 维度集带版本号（如 `aesthetic_v1`），任何修改产生新版本；跨版本数据不做维度级对比。
> - 新需求接入 = 在管理端配置一个新的维度集（维度名称、说明、引导语、打分范围、多语言文案），无需开发。

---

## 六、功能需求

### 6.1 模块一：管理端 · 评测任务创建（内部使用，需登录）

| 编号 | 需求 | 优先级 |
|------|------|:---:|
| A1 | 内部账号登录与权限管理（管理员/只读） | P0 |
| A2 | 创建评测任务：选择需求（绑定维度集及版本）、填写任务名称与说明 | P0 |
| A3 | **上传模型处理过后的照片**：批量上传图片文件（JPEG/PNG/WebP），单任务 10～500 张 | P0 |
| A4 | **上传模型打分数据**：随照片附带每张照片的模型总分 + 各维度分（JSON/CSV 模板，按文件名关联；或调用平台 API 写入） | P0 |
| A5 | 数据校验：照片与打分一一对应、分数在合法范围、维度与所选维度集版本一致；校验失败给出逐条错误提示 | P0 |
| A6 | 分享页内容规则配置：照片展示顺序（默认按模型美学总分降序，可选随机——随机可消除顺序偏差）、是否全部展示或仅展示 Top N | P0 |
| A7 | 分享页配置：链接有效期（默认 30 天）、目标打分人数、语言范围、是否允许同一用户重复参与 | P1 |
| A8 | 任务管理列表：任务状态（收集中/已结算）、实时打分进度（参与人数、已覆盖照片数）、手动关闭链接 | P0 |
| A9 | 维度集管理：新建/编辑维度集（名称、说明、引导语、打分范围、多语言文案），版本化保存 | P1 |

### 6.2 模块二：分享打分页（全球 NAS 用户，免注册）

| 编号 | 需求 | 优先级 |
|------|------|:---:|
| B1 | 打开唯一链接即可参与，**免注册、免安装**；以匿名会话标识区分打分者，可选填昵称 | P0 |
| B2 | 页面自动包含该任务的照片（按 A6 配置的顺序，例：算法按美学打分输出的照片序列）及本需求的打分维度 | P0 |
| B3 | 打分交互：每张照片给**总分**（滑杆/星级，归一化 0～100）+ 按**各维度**逐项打分（0～10）；维度打分可选，允许只打总分 | P0 |
| B4 | **盲评机制（默认开启）**：用户提交某张照片的打分之前，不展示模型分数；提交后即时揭晓"你 vs AI"的对比（提升趣味性与参与感），已提交的分数不可再改 | P0 |
| B5 | 轻量流畅：单张打分 ≤ 15 秒；支持滑动切换连续打分；断点续打（回到上次进度） | P0 |
| B6 | 允许部分完成：用户打了几张就算几张，数据均有效入库 | P0 |
| B7 | 多语言：界面与维度引导语支持中/英/日/德（首期），按浏览器语言自动切换，可手动更改 | P0 |
| B8 | 移动端与桌面端自适应；全球访问加速（CDN），首屏 ≤ 2s | P0 |
| B9 | 打分结束页：感谢语 + 个人小结（"你与 AI 的平均分差 X 分，最不一致的维度是 XX"），增强完成感 | P1 |
| B10 | 无效打分识别：全程同分、单张打分间隔 < 1 秒等机械行为标记为低质量样本，入库但在分析中降权/剔除 | P1 |

### 6.3 模块三：云端对比分析（模型分 vs 真人分）

打分数据回传云端后，由对比分析服务自动计算：

| 编号 | 需求 | 优先级 |
|------|------|:---:|
| C1 | **整体偏差**：全部照片上模型总分与真人总分（多人取均值，保留分布）的平均差值、差值方向、分布直方图 | P0 |
| C2 | **维度级偏差**：每个维度的平均偏差（模型偏高/偏低）与一致性指标（Spearman 相关系数） | P0 |
| C3 | **排序一致性**：模型 Top N 与真人 Top N 的重合率；整体排序相关性 | P0 |
| C4 | **分歧照片挖掘**：模型高估 Top 5 / 模型低估 Top 5，逐张列出双方总分与维度分差 | P0 |
| C5 | **分歧归因（云端大模型生成）**：对分歧照片自动生成归因假设，如"模型系统性低估多人合影类照片的『主体与瞬间』维度" | P1 |
| C6 | **人群一致性**：打分者之间的离散度（人类内部无共识的照片，模型偏差参考意义低，报告需标注）；按语言/地区分组的均值差异 | P1 |
| C7 | 结算触发：① 管理员手动结算；② 链接到期自动结算；③ 达到目标打分人数自动结算；结算前支持增量预览 | P0 |
| C8 | 数据门槛：单张照片至少 5 人打总分才纳入分析；有效打分覆盖不足 10 张照片时不生成报告，提示数据不足 | P0 |

### 6.4 模块四：自动生成对比报告（内部团队查看）

| 编号 | 需求 | 优先级 |
|------|------|:---:|
| D1 | 结算后自动生成报告，并通知任务创建者（站内 + 邮件） | P0 |
| D2 | 报告在管理端在线查看，固定模板 + 云端模型生成的动态结论文案 | P0 |
| D3 | 报告与原始打分数据可导出（报告 PDF；原始数据 CSV，供算法团队离线分析） | P0 |
| D4 | 同一需求下多次任务的横向对比（模型 v1 vs v2 的一致性变化趋势） | P2 |

**报告内容结构（v1 模板）**：

1. **总览**：照片数、有效打分人数与人次、模型分与真人分整体偏差（如"模型平均比真人高 6.3 分"）、整体一致性等级（高/中/低）。
2. **维度对比雷达图**：各维度上模型均分 vs 真人均分。
3. **偏差分布**：逐照片分差直方图。
4. **一致榜**：模型与真人都给高分/低分的照片（模型判断可信的区间）。
5. **分歧榜**：模型高估 Top 5、低估 Top 5，逐张展示照片、双方总分、维度分差。
6. **排序对比**：模型 Top 10 vs 真人 Top 10 的重合与错位。
7. **人群视角**：打分者内部一致性；按语言/地区分组的差异（样本足够时）。
8. **结论与建议**（自动文案）：例如"模型在色彩、光影维度与真人高度一致（ρ>0.8）；『主体与瞬间』维度系统性低估 1.8 分，建议补充人像抓拍类训练数据。"
9. **数据质量说明**：样本量、剔除的低质量打分比例、置信度提示。

---

## 七、非功能需求

| 类别 | 需求 |
|------|------|
| 性能 | 分享页首屏 ≤ 2s（全球 CDN）；打分提交响应 ≤ 500ms；报告生成 ≤ 60s |
| 规模 | 单任务照片 ≤ 500 张；单分享页并发访客 ≥ 500；单任务打分人数 ≥ 5000 |
| 可用性 | 打分页与数据回传服务可用性 99.9%；打分数据不丢失（提交即持久化） |
| 兼容 | 主流浏览器近两个大版本（Chrome/Safari/Edge/Firefox）与移动端 WebView |
| 国际化 | 界面、维度引导语、结束页文案多语言（首期中/英/日/德）；报告默认中文（P2：英文版） |
| 安全 | 分享链接 token 不可枚举（≥128bit）；管理端登录鉴权 + 操作审计；图片防盗链，展示分辨率上限可配置（默认长边 2048px） |

---

## 八、隐私与合规（打分者为全球用户，P0）

1. **免注册但合规**：打分者以匿名会话标识记录，不强制收集任何个人身份信息；昵称为可选自愿填写。
2. **知情同意**：打分页首屏以当地语言展示简短说明——打分数据将用于评估与改进模型，继续参与即视为同意；提供隐私政策链接。
3. **区域合规**：遵循 GDPR/CCPA 等；仅收集打分行为数据与浏览器语言/粗粒度地区（国家级），不收集精确定位；提供数据删除请求通道。
4. **照片侧合规**：评测照片由内部团队上传，须确保拥有使用与展示授权（内部素材库/已授权数据集）；上传时管理员需勾选版权确认；不得使用含可识别个人且未授权的照片。
5. **Cookie/本地存储**：仅用于断点续打的会话标识，按地区法规展示 Cookie 提示。

---

## 九、数据模型（概念）

- **Demand**：demand_id、名称、维度集引用。
- **DimensionSet**：set_id、version、维度列表（名称/说明/引导语/范围/多语言文案）。
- **Task**：task_id、demand_id、dimension_set_version、照片列表、分享页配置、状态。
- **Photo**：photo_id、task_id、文件、展示顺序。
- **ModelScore**：photo_id、总分、各维度分、模型版本。
- **RaterSession**：匿名 rater_id、语言、地区（国家级）、昵称（可选）、质量标记。
- **HumanScore**：photo_id、rater_id、总分、各维度分、时间戳、有效性标记。
- **Report**：task_id、统计结果、结论文案、生成时间。

---

## 十、关键指标

| 指标 | 定义 | 目标（上线 3 个月） |
|------|------|------|
| 任务创建成功率 | 创建任务→成功生成分享页 | ≥ 95% |
| 打分参与率 | 打开分享页后至少打 1 张的比例 | ≥ 40% |
| 人均打分张数 | 有效打分者平均完成照片数 | ≥ 15 张 |
| 打分完成率 | 打分者完成任务全部照片的比例 | ≥ 25% |
| 有效数据率 | 通过质量过滤的打分占比 | ≥ 85% |
| 报告生成率 | 结算任务成功产出报告的比例 | ≥ 95% |
| 需求接入效率 | 新需求（新维度集）接入耗时 | ≤ 1 天（纯配置） |

---

## 十一、里程碑规划（建议）

| 阶段 | 范围 | 周期 |
|------|------|------|
| M1（MVP） | 管理端任务创建与照片/模型分上传（A1–A6、A8）、分享打分页含盲评（B1–B8）、基础对比分析（C1–C4、C7、C8）、报告 v1（D1–D3，模板 1–6、9 节） | 6 周 |
| M2 | 维度集配置化（A9）、打分质量过滤（B10）、结束页个人小结（B9）、人群分组分析（C6）、报告人群视角 | +4 周 |
| M3 | 分歧归因自动生成（C5）、多任务横向对比（D4）、报告英文版 | +4 周 |

---

## 十二、风险与开放问题

| # | 风险/问题 | 影响 | 应对/待定 |
|---|----------|------|----------|
| 1 | 用户只打总分、跳过维度打分 | 维度级对比样本不足 | 维度打分可选但交互做轻（一屏多滑杆）；报告对样本不足的维度自动降级为"仅总分对比"并标注 |
| 2 | 锚定效应：先看到模型分会影响真人打分 | 对比结论失真 | 盲评为默认且提交后不可改（B4） |
| 3 | 免注册导致刷分/机器行为 | 数据污染 | 匿名会话+行为质量过滤（B10）、频控、同链接参与上限（A7） |
| 4 | 各国用户打分口径不一（宽严不同） | 均值失真 | 报告展示分布与人群一致性（C6）；后续版本考虑按打分者 z-score 归一化 |
| 5 | 照片展示顺序引入偏差（先看到的分更高） | 系统性误差 | A6 支持随机顺序；分析时按展示位次做敏感性检验（P2） |
| 6 | 美学高度主观，"人类真值"本身有噪声 | 结论可信度 | 单张照片 ≥5 人门槛（C8）；报告强制附数据质量说明 |
| 7 | 【待定】打分用户激励方案（积分/抽奖/纯自愿） | 参与率 | 待运营侧确认；MVP 先靠社区自愿 + 结束页趣味小结 |
| 8 | 【待定】分享页是否展示任务方品牌信息（我们作为能力提供方，链接可能由 NAS 厂商分发） | 品牌/合作 | 待商务确认，平台预留白标能力（P2） |

---

## 附录 A：模型打分数据上传模板（示意）

```json
{
  "task": "aesthetic-eval-202607",
  "dimension_set": "aesthetic_v1",
  "model_version": "aesthetic-model-2.3",
  "scores": [
    {
      "photo": "IMG_0001.jpg",
      "total": 87,
      "dimensions": {
        "构图": 9, "色彩": 8, "光影": 9,
        "清晰度与质感": 8, "主体与瞬间": 9, "氛围与情感": 8
      }
    }
  ]
}
```

## 附录 B：术语表

| 术语 | 说明 |
|------|------|
| 模型能力提供方 | 我们的角色：向 NAS 生态输出照片打分类模型能力的团队 |
| 盲评 | 用户提交打分前不展示模型分数的机制，避免锚定效应 |
| 分歧照片 | 模型分与真人分差距超过阈值的照片 |
| 结算 | 停止收集打分并触发对比分析与报告生成的动作 |
| 维度集版本 | 一组打分维度定义的版本号，模型分、真人分与报告必须一致 |
