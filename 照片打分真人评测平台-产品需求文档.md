# 照片打分真人评测平台（网页版）· 产品需求文档（PRD）

> 版本：v3.0（细化版）
> 日期：2026-07-09
> 状态：待评审
> 产品形态：网页（内部管理端 + 面向用户的分享打分页 + 报告页）
> 打分用户：全球 NAS 用户（免注册，打开链接即可参与）
> 本文示例需求：**照片美学打分模型效果评估**（平台按"需求"配置维度，本需求为首个落地示例）

---

## 一、背景与定位

### 1.1 我们是谁

我们是**算法能力提供方**：向 NAS 产品/生态输出照片处理与打分类的模型能力，例如照片美学打分模型。

### 1.2 要解决的问题

每一项模型能力上线前后，都需要回答同一个问题：

> **模型给照片打的分，和真实用户的判断到底差多少？差在哪些维度、哪些照片上？**

目前缺少标准化手段：

- 内部标注人少、成本高，且标注员审美不能代表全球 NAS 用户；
- 没有工具把"模型打分 → 真人打分 → 差距对比 → 结论报告"串成自动化链路；
- 每换一个评估需求（美学、清晰度、修图效果……）都要重搭一套收集与分析流程。

### 1.3 产品定位

一个网页版的**模型打分真人评测平台**，完整链路：

> **算法能力提供方（我们）**在管理端上传**模型处理过后的照片**和**模型各维度的打分** → 网站按该需求的**维度集**自动生成**分享页** → 全球 NAS 用户打开链接，对照片按相同维度**真人打分** → 网页调用**云端对比模型**，计算模型打分与用户真实打分的差距 → **自动生成对比报告**，呈现两者差别，供内部团队评估与迭代模型。

一句话：**用真实用户的打分，来给我们的模型打分。**

### 1.4 以"照片美学打分模型效果评估"为例的一次完整使用

1. 算法同学把美学模型 v2.3 跑过的 200 张照片，连同每张照片的模型总分和 6 个维度分，上传到平台，创建任务《美学模型 v2.3 效果评估·第 1 期》；
2. 平台校验数据后自动生成分享页链接，运营通过 NAS 产品推送和用户社区分发；
3. 全球 NAS 用户打开链接，逐张照片先自己打分（总分 + 各维度），提交后立刻看到"你 vs AI"的对比揭晓；
4. 7 天后（或达到目标人数）任务结算，云端对比模型完成分析；
5. 团队收到报告：整体偏差、维度雷达图、模型高估/低估的分歧照片榜、自动结论——"模型在色彩维度与真人高度一致，在『主体与瞬间』维度系统性低估 1.8 分"。

---

## 二、目标与非目标

### 2.1 产品目标

1. 内部团队 10 分钟内完成一次评测任务创建（上传照片与模型分 → 拿到分享页链接）。
2. 打分页对全球用户零门槛：免注册、免安装、移动端可用、多语言。
3. 分享页打分参与率（打开后至少给 1 张照片打分）≥ 40%。
4. 报告直接回答三个问题：**整体偏差多少？哪些维度不一致？哪些照片分歧最大？**——可直接用于模型迭代决策。
5. 平台按"需求"抽象：更换维度集即可支持新的评测需求，接入耗时 ≤ 1 天（纯配置，无开发）。

### 2.2 非目标（本期不做）

- 不做面向 C 端的照片管理/上传功能（照片仅由内部团队上传）。
- 不做模型推理服务（照片在上传前已由模型处理打分完毕，平台只接收结果文件）。
- 不做公开社区/照片广场（分享页仅限持有链接者访问）。
- 不做真人打分数据的实时模型训练（本期只做差距分析与报告，原始数据供算法团队离线使用）。

---

## 三、用户与角色

| 角色 | 是谁 | 使用哪端 | 核心行为 |
|------|------|---------|---------|
| 评测管理员 | 内部算法/产品/运营同学 | 管理端（需登录） | 创建任务、上传照片与模型分、配置分享页、结算、看报告 |
| 打分用户 | 全球 NAS 用户 | 分享打分页（免注册） | 打开链接，浏览照片，按维度打分 |
| 报告读者 | 内部算法/产品团队 | 管理端报告页 | 阅读对比报告，指导模型迭代 |

**打分用户说明**：通过 NAS 产品内推送、用户社区、邮件等渠道触达；来自不同国家/语言/文化背景，其审美多样性正是评测价值所在。平台记录打分者的浏览器语言与国家级地区（匿名，见 §10），供报告分组分析。

---

## 四、核心概念与总体流程

### 4.1 核心概念

| 概念 | 说明 |
|------|------|
| 需求（Demand） | 一类模型能力的评测场景，如"照片美学打分模型效果评估"。每个需求绑定一个**维度集** |
| 维度集（Dimension Set） | 该需求下的打分维度定义（名称、说明、引导语、打分范围），带版本号。模型分、真人分、报告必须使用同一版本 |
| 评测任务（Task） | 一次评测：一批照片 + 对应模型打分 + 一个分享页 + 一份报告 |
| 分享页（Share Page） | 由任务自动生成的免注册打分网页，唯一链接对外分发 |
| 对比报告（Report） | 云端模型对"模型分 vs 真人分"的自动分析结果 |

### 4.2 总体流程

```
【管理端 · 算法能力提供方（内部登录）】
  ① 创建任务：选择需求（例：照片美学打分模型效果评估 → 自动带出美学维度集 aesthetic_v1）
  ② 上传模型处理过后的照片（批量图片文件）
  ③ 上传模型各维度的打分（JSON/CSV，按文件名与照片关联）
  ④ 平台校验 → 预览分享页 → 发布，获得唯一链接
        │
        │ 链接经 NAS 产品推送 / 社区 / 邮件分发
        ▼
【分享打分页 · 全球 NAS 用户（免注册）】
  ⑤ 打开链接 → 看到算法按美学打分输出的照片序列
  ⑥ 逐张照片：先给总分 + 按美学各维度逐项打分（盲评）
  ⑦ 提交后即时揭晓该张照片的"你 vs AI"对比 → 滑到下一张
        │
        │ 打分数据提交即持久化，实时回传
        ▼
【云端 · 自动分析】
  ⑧ 任务结算（到期/达标/手动）→ 云端对比模型计算模型分 vs 真人分差距
  ⑨ 自动生成对比报告 → 通知内部团队
```

### 4.3 任务状态机

```
草稿(Draft) ──发布──→ 收集中(Collecting) ──结算──→ 分析中(Analyzing) ──完成──→ 已完成(Done)
   │                      │  ▲                                              
   └──删除──→ 已删除       │  └─(管理员可暂停/恢复链接)                        
                          └──手动关闭且不结算──→ 已关闭(Closed，无报告)
```

| 状态 | 说明 | 允许的操作 |
|------|------|-----------|
| 草稿 | 数据已上传但未发布，链接不可访问 | 编辑、预览、发布、删除 |
| 收集中 | 链接生效，接收打分 | 暂停/恢复链接、查看实时进度、手动结算、关闭 |
| 分析中 | 已停止收集，云端分析运行中 | 查看进度 |
| 已完成 | 报告已生成 | 查看/导出报告与原始数据 |
| 已关闭 | 未结算即关闭，仅保留原始数据 | 导出原始数据 |

---

## 五、示例需求：照片美学打分维度集（aesthetic_v1）

模型对每张照片输出以下维度的分数；用户在分享页上按**完全相同的维度**打分。

**总分**：0～100 分；**各维度**：0～10 分（整数）。

| # | 维度 | 维度 key | 说明 | 用户侧引导语（打分页展示） |
|---|------|---------|------|--------------------------|
| 1 | 构图 | composition | 主体位置、画面平衡、引导线、裁切 | 主体是否突出？画面是否协调不杂乱？ |
| 2 | 色彩 | color | 色调和谐度、饱和度、白平衡 | 颜色看起来是否舒服、有风格？ |
| 3 | 光影 | lighting | 曝光、对比度、光线运用 | 明暗是否合适？光影是否有层次？ |
| 4 | 清晰度与质感 | clarity | 对焦、噪点、动态模糊 | 主体是否清晰？画质是否干净？ |
| 5 | 主体与瞬间 | subject_moment | 拍摄对象的表情、姿态、抓拍时机 | 是否抓到了好瞬间？ |
| 6 | 氛围与情感 | mood | 画面传达的情绪与故事感 | 这张照片是否打动你？ |

> - 维度集带版本号（`aesthetic_v1`），任何修改产生新版本；跨版本数据不做维度级对比。
> - 新需求接入 = 在管理端配置一个新维度集（维度 key、名称、说明、引导语、打分范围、多语言文案），无需开发。

---

## 六、功能需求 · 管理端（内部使用，需登录）

### 6.1 页面清单

| 页面 | 说明 |
|------|------|
| M1 任务列表页 | 全部任务及状态、进度、入口 |
| M2 任务创建向导 | 四步：基本信息 → 上传照片 → 上传模型分 → 分享页配置与预览 |
| M3 任务详情页 | 实时进度、链接管理、结算操作 |
| M4 报告页 | 在线查看报告、导出 |
| M5 维度集管理页 | 维度集的创建、编辑、版本管理 |

### 6.2 功能明细

#### A. 账号与权限

| 编号 | 需求 | 优先级 |
|------|------|:---:|
| A1 | 内部账号登录（对接内部 SSO 或独立账号体系，二选一由技术评审定） | P0 |
| A2 | 两级权限：管理员（全部操作）、只读（仅查看任务与报告） | P1 |
| A3 | 关键操作审计日志：发布、关闭、结算、删除、导出 | P1 |

#### B. 任务创建向导（M2，四步）

**第 1 步 · 基本信息**

| 编号 | 需求 | 优先级 |
|------|------|:---:|
| B1 | 填写任务名称（必填，≤50 字）、说明（选填，≤500 字） | P0 |
| B2 | 选择需求 → 自动绑定维度集及当前版本（页面展示维度明细供确认） | P0 |
| B3 | 填写模型版本号（必填，如 `aesthetic-model-2.3`，用于报告与横向对比） | P0 |

**第 2 步 · 上传模型处理过后的照片**

| 编号 | 需求 | 优先级 |
|------|------|:---:|
| B4 | 批量拖拽/选择上传，支持 JPEG/PNG/WebP；单张 ≤ 20MB；单任务 10～500 张 | P0 |
| B5 | 上传中断可续传；逐张显示上传状态与失败原因，支持失败重传 | P0 |
| B6 | 平台自动生成打分页用的展示图（长边 2048px）与缩略图，保留原图仅供内部端查看 | P0 |
| B7 | 重复文件名检测：同名文件提示覆盖或跳过 | P1 |

**第 3 步 · 上传模型各维度的打分**

| 编号 | 需求 | 优先级 |
|------|------|:---:|
| B8 | 上传打分文件（JSON 或 CSV，模板见附录 A），按 `photo` 文件名与已上传照片关联；或通过开放 API 写入（P1） | P0 |
| B9 | 字段级校验（规则见 §6.3），校验失败给出逐条错误清单（行号 + 原因），可下载错误报告修正后重传 | P0 |
| B10 | 校验通过后展示汇总预览：照片数、模型总分分布直方图、各维度均值——供上传者确认数据没传错 | P1 |

**第 4 步 · 分享页配置与预览**

| 编号 | 需求 | 优先级 |
|------|------|:---:|
| B11 | 照片展示顺序：按模型总分降序 / 随机（推荐，消除顺序偏差）/ 自定义；默认**随机** | P0 |
| B12 | 展示范围：全部照片 / 仅模型分 Top N（N 可填） | P0 |
| B13 | 每位用户的打分张数：全部 / 随机抽取 K 张（大任务下降低单人负担，系统保证各照片被均匀覆盖） | P1 |
| B14 | 链接有效期（默认 30 天）、目标打分人数（达到即自动结算，可不填）、同一用户是否可重复参与（默认否） | P0 |
| B15 | 语言范围（默认中/英/日/德全开）与打分页欢迎语（多语言文案，提供默认模板） | P1 |
| B16 | 桌面/移动双端**实时预览**分享页，确认后发布，生成唯一链接与二维码 | P0 |

#### C. 任务详情与进度（M3）

| 编号 | 需求 | 优先级 |
|------|------|:---:|
| C1 | 实时进度：访问数、参与人数（≥1 张打分）、总打分人次、逐照片覆盖人数（最低/平均） | P0 |
| C2 | 链接管理：复制链接/二维码、暂停/恢复、重置链接（旧链接失效） | P0 |
| C3 | 结算：手动结算按钮 + 自动结算（到期或达到目标人数）；结算前展示数据是否达到分析门槛（§8 C8） | P0 |
| C4 | 收集中可增量预览关键指标（当前整体偏差、参与量走势），标注"数据未结算，仅供参考" | P1 |

#### D. 维度集管理（M5）

| 编号 | 需求 | 优先级 |
|------|------|:---:|
| D1 | 新建/编辑维度集：维度 key、名称、说明、引导语、打分范围、多语言文案；保存即生成新版本号 | P1 |
| D2 | 已被任务引用的维度集版本不可修改、不可删除 | P1 |

---

## 七、功能需求 · 分享打分页（全球 NAS 用户，免注册）

### 7.1 页面结构（三个界面态）

```
① 欢迎页                ② 打分卡片（核心，逐张循环）           ③ 结束页
┌────────────┐        ┌──────────────────────┐        ┌────────────┐
│ 活动标题     │        │  照片（可点击放大）      │        │ 感谢参与     │
│ 玩法说明     │        │──────────────────────│        │ 个人小结：    │
│ 隐私声明     │  →     │ 总分滑杆  0-100        │   →    │ 你 vs AI    │
│ [开始打分]   │        │ ▾ 维度打分(可展开,选填) │        │ 平均差X分    │
│ 语言切换     │        │  构图 ○○○○○○○○○○      │        │ 最不一致维度  │
└────────────┘        │  色彩 ○○○○○○○○○○ …    │        │ [重新回顾]   │
                      │ [提交本张]  进度 3/20   │        └────────────┘
                      │──────────────────────│
                      │ 提交后原位揭晓：         │
                      │ 你 78 | AI 87 (+9)     │
                      │ 维度差异迷你条形图        │
                      │ [下一张 →]             │
                      └──────────────────────┘
```

### 7.2 功能明细

| 编号 | 需求 | 优先级 |
|------|------|:---:|
| E1 | 打开唯一链接即可参与，免注册免安装；以匿名会话标识（本地存储）区分打分者，可选填昵称 | P0 |
| E2 | 欢迎页：活动标题、玩法说明（一句话 + 预计耗时）、简短隐私声明与政策链接、语言切换 | P0 |
| E3 | 照片按任务配置顺序（B11）逐张展示；照片可点击放大、双指缩放（移动端） | P0 |
| E4 | **总分**：滑杆 0～100，无默认值（用户必须主动滑动，避免默认值偏差）；未打总分不可提交 | P0 |
| E5 | **维度打分**：6 个维度各 0～10（点选圆点或滑杆），默认收起、点击展开；选填，允许只打总分 | P0 |
| E6 | **盲评机制（默认开启）**：提交前不展示任何模型分数；点击"提交本张"后原位揭晓"你 vs AI"总分对比及维度差异迷你图；**已提交不可修改** | P0 |
| E7 | 跳过：每张照片提供"跳过"入口（不产生数据），跳过率计入数据质量统计 | P1 |
| E8 | 断点续打：中途关闭页面，同设备再次打开回到上次进度；已打分照片不重复出现 | P0 |
| E9 | 部分完成即有效：打几张算几张，数据均入库 | P0 |
| E10 | 结束页个人小结："你与 AI 的平均分差 X 分；最一致维度 XX，最不一致维度 XX"，并可回顾每张对比 | P1 |
| E11 | 多语言：界面、维度引导语、欢迎语支持中/英/日/德，按浏览器语言自动切换、可手动改 | P0 |
| E12 | 性能与兼容：首屏 ≤ 2s（全球 CDN）；照片预加载下一张；提交响应 ≤ 500ms；移动端与桌面自适应；主流浏览器近两个大版本 | P0 |
| E13 | 无效打分识别：全程同分、单张停留 < 2 秒、打分间隔 < 1 秒等机械行为打上低质量标记，入库但分析时剔除/降权 | P1 |
| E14 | 防滥用：同链接单会话打分频控；重置链接后旧会话失效 | P1 |

### 7.3 边界与异常场景

| 场景 | 表现 |
|------|------|
| 链接已过期/已关闭/已重置 | 展示"活动已结束"页（多语言），不报错 |
| 任务收集中被暂停 | 展示"活动暂停，请稍后再来"，已打分数据保留 |
| 照片加载失败 | 自动重试 2 次，仍失败则跳过该张并上报，不阻塞流程 |
| 打分提交失败（网络） | 本地暂存，自动重试，恢复后补传；用户无感知 |
| 用户已完成全部照片后再次打开 | 直接进入结束页（个人小结） |
| 不允许重复参与时清除了本地存储 | 按新会话处理（已知局限，配合 E13/E14 控制影响，见风险 #3） |

---

## 八、功能需求 · 云端对比分析（模型分 vs 真人分）

任务结算后，云端对比模型按以下口径自动计算：

### 8.1 计算口径

记第 i 张照片：模型总分 `M_i`，第 j 位有效打分者总分 `H_ij`，真人均分 `H̄_i = mean_j(H_ij)`。

| 编号 | 指标 | 计算口径 | 优先级 |
|------|------|---------|:---:|
| C1 | 整体偏差 | `mean_i(M_i − H̄_i)`（含方向）；`mean_i |M_i − H̄_i|`（幅度）；分差直方图 | P0 |
| C2 | 维度级偏差 | 每维度同 C1 口径的均值偏差 + Spearman 相关系数 ρ（模型维度分 vs 真人维度均分，跨照片） | P0 |
| C3 | 排序一致性 | 模型 Top N 与真人 Top N 重合率（N=10、20）；全量排序 Spearman ρ | P0 |
| C4 | 分歧照片 | 按 `|M_i − H̄_i|` 降序：模型高估 Top 5（M−H̄ 最大）、低估 Top 5（最小），附维度分差明细 | P0 |
| C5 | 分歧归因 | 云端大模型对分歧照片生成归因假设（结合照片内容与维度差模式），如"模型系统性低估多人合影的『主体与瞬间』维度"；标注为 AI 生成、仅供参考 | P1 |
| C6 | 人群一致性 | 逐照片真人打分标准差；打分者两两一致性（ICC 或平均 Spearman）；按语言/地区分组均值差（单组样本 ≥ 30 人才展示） | P1 |
| C7 | 结算触发 | ① 管理员手动；② 链接到期自动；③ 达到目标人数自动 | P0 |
| C8 | 数据门槛 | 单张照片 ≥ 5 人有效总分才纳入分析；纳入分析的照片 < 10 张则不出报告，提示"数据不足"并给出缺口明细 | P0 |
| C9 | 质量过滤 | E13 标记的低质量打分剔除；报告披露剔除比例 | P0 |

### 8.2 一致性等级定义（报告用）

| 等级 | 判定（总分排序 ρ） | 含义 |
|------|------------------|------|
| 高 | ρ ≥ 0.7 | 模型排序基本可替代真人初筛 |
| 中 | 0.4 ≤ ρ < 0.7 | 模型可用于粗筛，关键场景需人工复核 |
| 低 | ρ < 0.4 | 模型与该人群审美偏差大，需针对性迭代 |

---

## 九、功能需求 · 自动生成对比报告（内部团队查看）

| 编号 | 需求 | 优先级 |
|------|------|:---:|
| R1 | 结算完成后自动生成报告，通知任务创建者（站内 + 邮件） | P0 |
| R2 | 管理端在线查看：固定模板 + 云端模型生成的动态结论文案 | P0 |
| R3 | 导出：报告 PDF；原始打分数据 CSV（含匿名 rater_id、语言、地区、质量标记，供算法团队离线分析） | P0 |
| R4 | 同一需求下多任务横向对比：模型 v1 vs v2 的整体偏差与各维度 ρ 变化趋势 | P2 |

### 9.1 报告模板（v1，共 9 节）

1. **总览卡片**：照片数、有效打分人数/人次、整体偏差（如"模型平均比真人高 6.3 分"）、一致性等级（高/中/低，含判定依据）。
2. **维度对比雷达图**：各维度模型均分 vs 真人均分双层雷达。
3. **偏差分布**：逐照片分差直方图与散点图（模型分 × 真人均分）。
4. **一致榜**：双方都给高分/低分的照片——模型判断可信的区间。
5. **分歧榜**：模型高估 Top 5、低估 Top 5，逐张展示照片缩略图、双方总分、维度分差条形图。
6. **排序对比**：模型 Top 10 vs 真人 Top 10 重合与错位可视化。
7. **人群视角**：真人内部一致性；按语言/地区分组差异（样本足够时）。
8. **结论与建议**（自动文案，标注 AI 生成）。
9. **数据质量说明**：样本量、逐照片覆盖分布、低质量打分剔除比例、置信度提示。

### 9.2 报告结论文案样例（示意）

> 本期共 200 张照片、312 名有效打分者、4,180 人次打分（剔除低质量打分 6.2%）。
> **整体**：模型总分平均比真人高 6.3 分，排序一致性 ρ=0.71，等级"高"。
> **维度**：色彩（ρ=0.83）、光影（ρ=0.79）与真人高度一致；**『主体与瞬间』系统性低估 1.8 分（ρ=0.42）**，低估集中在多人合影与儿童抓拍类照片。
> **建议**：色彩、光影维度打分可直接用于产品侧排序；建议补充人像抓拍类训练数据，并复核『主体与瞬间』维度的特征设计。
> **人群**：日语区用户总体打分偏严（均值低 4.1 分），但排序与其他地区一致，不影响结论。

---

## 十、隐私与合规（打分者为全球用户，P0）

1. **免注册但合规**：打分者以匿名会话标识记录，不强制收集任何个人身份信息；昵称自愿填写。
2. **知情同意**：欢迎页以当地语言说明"打分数据将用于评估与改进模型"，附隐私政策链接；继续参与视为同意。
3. **区域合规**：遵循 GDPR/CCPA 等；仅收集打分行为、浏览器语言、国家级地区，不收集精确定位；提供数据删除请求通道。
4. **照片侧合规**：评测照片由内部团队上传，须为内部素材库或已授权数据集；上传流程强制勾选版权与授权确认；不得使用含可识别个人且未授权的照片。
5. **Cookie/本地存储**：仅用于断点续打的会话标识，按地区法规展示提示。
6. **传输与存储**：全程 HTTPS；打分数据与照片存储加密；分享链接 token ≥ 128bit 不可枚举；打分页图片防盗链。

---

## 十一、数据模型（概念）

- **Demand**：demand_id、名称、dimension_set 引用。
- **DimensionSet**：set_id、version、维度列表（key/名称/说明/引导语/范围/多语言文案）。
- **Task**：task_id、demand_id、dimension_set_version、model_version、状态、分享页配置（顺序/范围/有效期/目标人数/语言）。
- **Photo**：photo_id、task_id、原图/展示图/缩略图、展示顺序。
- **ModelScore**：photo_id、total、各维度分、model_version。
- **RaterSession**：匿名 rater_id、语言、地区（国家级）、昵称（可选）、质量标记。
- **HumanScore**：photo_id、rater_id、total、各维度分、停留时长、时间戳、有效性标记。
- **Report**：task_id、统计结果（§8 全部指标）、结论文案、生成时间。

---

## 十二、关键指标

| 指标 | 定义 | 目标（上线 3 个月） |
|------|------|------|
| 任务创建成功率 | 创建任务→成功发布分享页 | ≥ 95% |
| 打分参与率 | 打开分享页后至少打 1 张的比例 | ≥ 40% |
| 人均打分张数 | 有效打分者平均完成照片数 | ≥ 15 张 |
| 维度打分渗透率 | 打了总分的人中同时打维度分的比例 | ≥ 50% |
| 有效数据率 | 通过质量过滤的打分占比 | ≥ 85% |
| 报告生成率 | 结算任务成功产出报告的比例 | ≥ 95% |
| 需求接入效率 | 新需求（新维度集）接入耗时 | ≤ 1 天（纯配置） |

---

## 十三、里程碑规划（建议）

| 阶段 | 范围 | 周期 |
|------|------|------|
| M1（MVP） | 管理端：A1、B1–B12、B14、B16、C1–C3；打分页：E1–E6、E8、E9、E11、E12 及异常场景；分析：C1–C4、C7–C9；报告：R1–R3（模板 1–6、9 节） | 6 周 |
| M2 | 维度集配置化（D1、D2）、抽样打分（B13）、质量过滤（E13、E14）、跳过（E7）、结束页小结（E10）、人群分析（C6）与报告第 7 节、增量预览（C4/B10） | +4 周 |
| M3 | 分歧归因（C5）与报告第 8 节增强、多任务横向对比（R4）、报告英文版、开放 API 写入模型分 | +4 周 |

---

## 十四、风险与开放问题

| # | 风险/问题 | 影响 | 应对/待定 |
|---|----------|------|----------|
| 1 | 用户只打总分、跳过维度打分 | 维度级对比样本不足 | 维度打分默认收起但一屏可完成；结束页小结用维度差异做正反馈；报告对样本不足的维度自动降级为"仅总分对比"并标注 |
| 2 | 锚定效应：先看到模型分会影响真人打分 | 对比结论失真 | 盲评为默认且提交后不可改（E6） |
| 3 | 免注册导致刷分/重复参与 | 数据污染 | 匿名会话 + 行为质量过滤（E13）、频控（E14）、链接可重置；报告披露剔除比例 |
| 4 | 各国用户打分口径不一（宽严不同） | 均值失真 | 报告展示分布与人群一致性（C6）；后续版本按打分者 z-score 归一化 |
| 5 | 照片展示顺序引入位置偏差 | 系统性误差 | 默认随机顺序（B11）；P2 做按展示位次的敏感性检验 |
| 6 | 美学高度主观，"人类真值"本身有噪声 | 结论可信度 | 单张 ≥ 5 人门槛（C8）；报告强制附数据质量说明；人群一致性低的照片单独标注 |
| 7 | 【待定】打分用户激励方案（积分/抽奖/纯自愿） | 参与率 | 待运营确认；MVP 先靠社区自愿 + 揭晓互动与个人小结 |
| 8 | 【待定】分享页品牌露出（链接可能由 NAS 厂商分发，我们是能力提供方） | 品牌/合作 | 待商务确认；平台预留白标能力（P2） |
| 9 | 【待定】单任务照片较多（如 500 张）时是否强制启用抽样打分（B13） | 单人负担/覆盖均匀性 | 建议 >50 张即默认抽样 K=20，待评审确认 |

---

## 附录 A：模型打分数据上传模板

**JSON 模板**（推荐）：

```json
{
  "task": "aesthetic-eval-202607",
  "dimension_set": "aesthetic_v1",
  "model_version": "aesthetic-model-2.3",
  "scores": [
    {
      "photo": "IMG_0001.jpg",
      "total": 87,
      "dimensions": {
        "composition": 9, "color": 8, "lighting": 9,
        "clarity": 8, "subject_moment": 9, "mood": 8
      }
    }
  ]
}
```

**CSV 模板**：

```csv
photo,total,composition,color,lighting,clarity,subject_moment,mood
IMG_0001.jpg,87,9,8,9,8,9,8
```

**校验规则**（B9）：

| 规则 | 失败提示 |
|------|---------|
| `photo` 必须与已上传照片文件名一一对应 | 列出缺打分的照片、缺照片的打分行 |
| `total` ∈ [0,100]，维度分 ∈ [0,10]，数值型 | 行号 + 字段 + 实际值 |
| 维度 key 必须与所选维度集版本完全一致（不多、不少、不错名） | 列出多余/缺失/未知的 key |
| `dimension_set` 与任务所选版本一致 | 版本不匹配提示 |
| 同一 `photo` 不得重复出现 | 重复行号 |

## 附录 B：术语表

| 术语 | 说明 |
|------|------|
| 算法能力提供方 | 我们的角色：向 NAS 生态输出照片打分类模型能力的团队 |
| 盲评 | 用户提交打分前不展示模型分数的机制，避免锚定效应 |
| 揭晓 | 用户提交单张打分后原位展示"你 vs AI"对比的交互 |
| 分歧照片 | 模型分与真人均分差距最大的照片 |
| 结算 | 停止收集打分并触发对比分析与报告生成的动作 |
| 维度集版本 | 一组打分维度定义的版本号，模型分、真人分与报告必须一致 |
