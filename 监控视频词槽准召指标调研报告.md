# 监控视频文案词槽准召指标定义调研报告

> 版本：v1.1 ｜ 日期：2026-07
> 适用场景：监控中心 App，对监控录像连续抽帧后由端侧大模型生成结构化文案
> 目标模型：Qwen3.5-4B（多模态，INT4 量化）
> 目标硬件：Intel Core Ultra 5（CPU + iGPU + NPU），32GB 内存
> 调研方法：基于公开论文、学术基准测试集（benchmark）、国家/行业标准、厂商公开产品指标综合推导
>
> **v1.1 修订说明**：回应评审质疑——v1.0 部分目标值参照的是检测/判别式模型基准，未充分区分大模型"图生文"的实际生成效果。本版新增：①三种测量口径的定义与区分（§2.4）；②生成式口径的公开实测数据（§3.6）；③每个词槽在"纯 4B 自由生成 / 槽位约束生成 / 小模型+大模型混合"三种口径下的可达值对照（§1.2）；④推荐的小模型+大模型结合出文案的架构设计（§5）。**结论：v1.0 的目标值仅在混合架构下可达，纯自由图生文无法达标，两者差距有公开数据定量支撑。**

---

## 一、结论速览

### 1.1 推荐指标总表（适用口径：小模型+大模型混合架构，见 §5）

指标分两档：**基线（上线门槛）** 和 **目标（迭代方向）**，写法为 `基线 → 目标`。
分级：**S = 安全关键**（漏报有安全后果，召回优先）；**A = 核心体验**（文案主干，精确优先）；**B = 增强信息**（可选槽，允许输出"未知"）。

| 词槽 | 子类 | 级别 | 精确率 P | 召回率 R | 补充约束 |
|------|------|:---:|:---:|:---:|------|
| **主体** | 人形存在（有人/无人） | S | 95% → 97% | 95% → 97% | 误报 ≤1 次/路/天 |
| | 熟人（人脸库命中，含姓名） | S | 95% → 97% | 80% → 88% | 陌生人误认成熟人属 P 错误，从严 |
| | 人群属性（中年男/女、儿童、老人） | A | 82% → 88% | 78% → 85% | 不确定时退化为"陌生人" |
| | 职业角色（快递员/外卖员） | A | 82% → 88% | 65% → 75% | 仅制服/装备可见时判定 |
| | 车辆（类型+颜色，如黑色SUV） | A | 88% → 92% | 82% → 88% | 夜间红外颜色输出"未知" |
| | 宠物（狗/猫） | A | 88% → 92% | 82% → 88% | |
| | 包裹存在 | A | 88% → 92% | 80% → 88% | |
| | 包裹品牌（美团/顺丰/京东） | B | 82% → 88% | 55% → 70% | 低置信输出"包裹"不带品牌 |
| **属性** | 衣着颜色 | B | 85% → 90% | 75% → 82% | 幻觉率 ≤5%；红外画面强制"未知" |
| | 佩戴（口罩/帽子/眼镜） | B | 85% → 90% | 78% → 85% | 眼镜类小目标允许放宽 |
| | 体型（偏瘦/偏胖） | B | 72% → 80% | 55% → 65% | 仅高置信输出，默认不输出 |
| **行为** | 摔倒 | S | 82% → 88% | **93% → 96%** | 误报 ≤0.5 次/路/天；召回绝对优先 |
| | 攀爬 | S | 82% → 88% | **90% → 95%** | |
| | 徘徊 | S | 85% → 90% | 88% → 93% | 依赖"跟踪时长≥阈值"规则联动 |
| | 交互动作（按铃/取走/放置） | A | 82% → 88% | 70% → 80% | 按铃优先用门铃传感器信号兜底 |
| | 基础动作（出现/经过/进入/穿越/停留/奔跑/静止） | A | 88% → 93% | 82% → 90% | 主要由检测+轨迹规则计算 |
| **状态** | 站着/坐着/躺着 | A | 85% → 90% | 82% → 88% | "躺着"参与摔倒复核时按 S 级召回 |
| | 蹲着/趴着/抱着 | B | 78% → 85% | 68% → 78% | |
| **场景** | 固定机位（安装时标定） | A | 95% → 98% | 95% → 98% | 本质是配置项+变化检测 |
| | 纯模型判定（云台/移动机位） | A | 85% → 90% | 82% → 88% | 闭集 ~15 类词表 |
| **时长** | 分桶口径（约N秒/约N分钟） | A | 分桶准确率 82% → 88% | — | 长事件（≥20s）相对误差≤±25%；短事件绝对误差≤±5s |

**端到端综合指标**（在词槽指标之上另设）：

| 综合指标 | 基线 | 目标 | 说明 |
|---------|:---:|:---:|------|
| 文案可用率 | ≥88% | ≥93% | 单条文案中 S/A 级词槽无一错误的比例 |
| 关键幻觉率 | ≤5% | ≤3% | 文案捏造了画面中不存在的主体/行为的比例 |
| S 级事件端到端召回 | ≥92% | ≥95% | 小模型触发 × 大模型复核保留率的乘积口径 |
| 推送误报量 | ≤1 次/路/天 | ≤0.5 次/路/天 | 工程口径，对齐消费级摄像头竞品水平 |

### 1.2 三种口径下的可达值对照（v1.1 新增，回应"按生成效果定指标"的质疑）

同一词槽，用三种方式让系统输出，可达水平差异巨大。下表给出各词槽在三种口径下**基于公开实测数据推算的可达值区间**（依据见 §3.6）：

| 词槽 | ①纯 4B 自由图生文<br>（一段 prompt 直接生成整段文案） | ②4B 槽位约束生成<br>（逐槽闭集问答+受限解码） | ③小模型+大模型混合<br>（§5 架构，推荐） |
|------|:---:|:---:|:---:|
| 人形存在 | P 75~85 / R 50~65 | P 85~90 / R 80~88 | **P 95+ / R 95+** |
| 熟人（人脸库） | **不可行**（4B VLM 无人脸底库比对能力） | 不可行 | **P 95 / R 80~88**（人脸小模型） |
| 人群属性 | P 70~80 / R 40~55 | P 78~85 / R 72~80 | P 82~88 / R 78~85 |
| 职业角色 | P 65~78 / R 35~50 | P 75~82 / R 60~72 | P 82~88 / R 65~75 |
| 车辆类型+颜色 | P 72~82 / R 45~60 | P 80~88 / R 72~82 | P 88~92 / R 82~88 |
| 包裹存在/品牌 | P 65~80 / R 30~50 | P 78~86 / R 60~75 | P 88 / R 80（品牌 R 55~70） |
| 衣着颜色 | P 70~80 / R 40~55（幻觉高发维度） | P 80~88 / R 68~78 | P 85~90 / R 75~82 |
| 佩戴 | P 70~80 / R 40~55 | P 80~87 / R 70~80 | P 85~90 / R 78~85 |
| 行为-摔倒 | P 55~70 / R 40~60 | P 70~82 / R 75~85 | **P 82~88 / R 93~96** |
| 行为-徘徊/停留（时序类） | **不可行**（依赖跨事件跟踪时长，VLM 无法从抽帧判定） | 不可行 | P 85~90 / R 88~93（跟踪规则） |
| 行为-瞬时交互（按铃/取走/放置） | P 45~60 / R 30~45 | P 65~78 / R 55~70 | P 82~88 / R 70~80 |
| 行为-基础动作 | P 60~75 / R 40~55 | P 72~82 / R 65~78 | P 88~93 / R 82~90（轨迹规则） |
| 状态（姿态） | P 68~80 / R 45~60 | P 78~86 / R 72~82 | P 85~90 / R 82~88 |
| 场景 | P 80~88 / R 60~75（VLM 强项） | P 85~92 / R 82~90 | P 95+ / R 95+（机位标定） |
| 时长 | **不可行**（VLM 无计时能力，计数/时长是公认最弱维度） | 不可行 | 分桶准确率 82~88（跟踪 ID 累计） |
| **整段文案事实精确率** | 65~75 | 82~90 | ≥88（可用率口径） |
| **整段文案要点召回** | 35~50 | 70~82 | ≥85 |

三条硬结论：

1. **纯自由图生文不达标，且差距不是调优能弥补的**：视频描述事实性基准 DREAM-1K 上，专门为视频描述训练的 SOTA 模型 Tarsier2-7B 事件级 F1 仅 42%（R 48 / P 35~40），GPT-4o 仅 39.2%；通用描述基准 CAPability 上 7B 开源最佳（Qwen2.5-VL-7B）自由描述精确率 75.5%、召回 48.1%、F1 54.8%。4B 端侧模型只会更低。**自由生成的瓶颈在召回（该说的漏说）和时序/计数类幻觉，这是生成范式的固有属性。**
2. **槽位约束生成可把大模型拉回"分类器"水平**：CAPability 发现模型存在大量"知而不述"（K̄T，know but cannot tell）——问答能答对、自由描述却漏掉。改成逐槽闭集问答后，性能从"caption 召回"回升到"VQA/零样本分类"水平（如零样本动作识别 Kinetics-400 top-1 约 76%），但仍普遍比专用小模型低 5~15 个点，且时序类槽位（徘徊/时长）依然不可行。
3. **因此 §1.1 的目标值只对混合架构（口径③）有效**；若产品决定用纯 VLM 方案，应按口径②的区间重新签署目标，并砍掉熟人/徘徊/时长三个槽或改由其他信号源提供。

---

## 二、指标口径定义（先定义、再定值）

准召数字没有意义，除非匹配规则先说清楚。推荐采用**两级判定**：

### 2.1 第一级：事件级匹配

生成文案对应一个"事件"（一段连续抽帧+跟踪 ID）。与人工标注的 GT 事件按以下条件匹配：

- 时间窗交并比（temporal IoU）≥ 0.5；
- 主体大类一致（人/车/动物/物品四选一对得上）。

- **事件召回率** = 匹配上的 GT 事件数 / GT 事件总数
- **事件精确率** = 匹配上的生成事件数 / 生成事件总数（不匹配的生成事件即"整条幻觉"）

### 2.2 第二级：词槽级准召（在匹配成功的事件内统计）

对每个词槽 k：

- **P_k（精确率）** = 该词槽输出正确的事件数 / 该词槽**有输出**的事件数
- **R_k（召回率）** = 该词槽输出正确的事件数 / GT 中该词槽**可判定**的事件数

关键规则：

1. **"未知"不算错也不算对**：输出"未知"不计入 P 的分母，但计入 R 的分母（GT 可判定而模型放弃 = 漏）。这是可选词槽（属性、品牌）敢设低召回、高精确的机制基础。
2. **闭集词表判定**：主体/行为/状态/场景按词表精确匹配；同义词归一化表（如"跑步"→"奔跑"）在评测前固化。
3. **多值词槽微平均**：属性词槽一个事件可有多个取值（"穿黑色衣服的、戴口罩的"），按属性项微平均计入 P/R。
4. **时长词槽用容差而非精确匹配**：分桶一致（约N秒/约N分钟/持续N分钟三桶+数值档位）即算对；或数值口径下 ≥20s 事件相对误差 ≤±25%、<20s 事件绝对误差 ≤±5s。
5. **GT"无法判定"的样本剔除**：画面模糊、遮挡导致人工也无法标注的槽位，不进入该槽分母（与本仓库《图片描述质量度量方案》"无法核实不扣分"原则一致）。
6. **补充指标——幻觉率**：对主体和属性槽单独统计"输出了画面中不存在的取值"的比例。小模型（4B 级）的幻觉风险显著高于大模型，这是与传统 CV 小模型评测最大的口径差异，必须单列。

### 2.3 工程口径的误报定义

安防行业（含消费级摄像头竞品）对误报通用口径不是 1−P，而是**误报次数/路/天**。原因是监控视频负样本时长远大于正样本（类别极度不平衡），比例型指标失真。建议对 S 级行为槽双口径并列：P/R 用于模型迭代，次/路/天用于产品验收。

### 2.4 三种测量口径的定义（v1.1 新增）

同一套 P/R 公式，测的对象不同，结果不可比。验收和对外承诺必须注明口径：

| 口径 | 被测对象 | 测法 | 对应学术基准类型 |
|------|---------|------|----------------|
| **①自由生成口径** | VLM 直接生成的整段文案 | 从文案中抽取断言，逐条与 GT 比对（AutoDQ/CAPability 式） | DREAM-1K、CAPability、VDC |
| **②槽位约束口径** | VLM 逐槽闭集问答的结构化输出（JSON） | 每槽独立判分（§2.2 规则） | VQA/零样本分类（MVBench、Kinetics zero-shot、POPE） |
| **③系统混合口径** | 小模型+VLM+规则融合后的最终词槽值 | 每槽独立判分（§2.2 规则） | 检测/跟踪/人脸/跌倒等专用基准 + ② |

v1.0 报告的问题在于用③的依据给出了指标，但未声明口径也未给出①②的可达值。本版 §1.2 已补齐。**最终文案的评测一律在词槽层（②/③口径）进行，文案文本本身由模板拼接产生、不引入新事实（见 §5.4），因此不再需要对文本做开放式事实核查。**

---

## 三、调研依据：各领域公开基准数据

### 3.1 词槽→学术任务映射与 SOTA 水平（判别式口径，用于口径③）

| 词槽 | 对应学术任务 | 代表基准 | 公开 SOTA 水平 | 来源 |
|------|-------------|---------|---------------|------|
| 主体-人形 | 行人检测 | CrowdHuman（密集遮挡最难场景） | 改进 YOLOv8 mAP50 78.4%（家庭场景遮挡少，实际更高） | MDPI Electronics 2024 |
| 主体-熟人 | 人脸验证/视频人脸 | IJB-C、NIST FRTE FIVE | TAR 97.7% @FAR=1e-4（近正面）；视频监控角度/距离下显著下降 | WebFace260M, NIST |
| 主体-人群属性 | 年龄组分类 | Adience、UTKFace | Adience 约 89.5%（8 分组）；UTKFace 粗分组 94~95% | EfficientNet 对比研究 2025 |
| 主体-车辆 | 车辆颜色/类型识别 | VeRi、VCR 基准 | 颜色 Top-1 约 94~95%；类型 95%+ | arXiv 2408.11589 |
| 属性 | 行人属性识别 PAR | PA-100K、PETA、RAPv1 | SOTA（LLM-PAR 等）F1 ≈ 86~90，P ≈ 88~90，R ≈ 86~89 | arXiv 2408.09720 |
| 行为-摔倒 | 跌倒检测 | UR Fall、UP-Fall、电梯场景 | 实验室数据集 94~99%+；贴近实战的 YOLO 方案 P 88.8~92.7% / R 91.4~94.1% | arXiv 2309.07154, MDPI 2023/2024 |
| 行为-徘徊/入侵 | 行为分析/异常检测 | 实际部署系统、UCF-Crime | 实际系统违规检出率 97.1%、精确率 89.2%；UCF-Crime AUC SOTA 88~90% | IIETA 2024, WACV 2026 |
| 行为-细粒度动作 | 时序动作定位 TAL | THUMOS14、ActivityNet | THUMOS14 平均 mAP ≈ 74%；ActivityNet ≈ 38~43% —— **带时间边界的细粒度动作远未解决** | arXiv 2407.13078 |
| 状态 | 姿态分类 | 各姿态数据集 | 总体 91~96%；分姿态差异大：站 98.3%、蹲 94.4%、弯腰/过渡姿态仅 75.9% | ACM ICMSIE 2020, PLOS One 2024 |
| 场景 | 场景分类 | Places365 | 365 类开放集 Top-1 仅 ~56%；**闭集 15 类 + 固定机位容易得多** | CSAILVision/places365 |
| 时长 | 多目标跟踪 MOT | MOT17/MOT20 | BoT-SORT 等 SOTA：MOTA 80.5、IDF1 80.2 —— 断轨导致时长系统性低估 | arXiv 2206.14651 |

### 3.2 Qwen3.5-4B 模型能力

- Qwen3.5 系列 0.8B/2B/4B/9B 于 2026-03 以 Apache 2.0 开源，4B 为原生多模态（文本+图像+视频），262K 上下文。
- 视频理解：Qwen3.5-4B 在 MLVU 达 **76.2**（超过上一代 Qwen3-VL-4B 的 75.7）；VideoMME（带字幕）保留同家族 27B 旗舰约 **96%** 的分数；OCR 85.4。
- 参照 Qwen3-VL 技术报告：8B 档即可对齐上一代 72B 的视频理解水平——4B 档的"能力折扣"已缩小到 5~10 个点。**但注意：这些是选择题/问答式基准（口径②），不代表自由生成文案的事实性（口径①），两者差距见 §3.6。**

### 3.3 国家/行业标准可对标项

- **GB/T 31488（安全防范视频监控人脸识别系统技术要求）**：标准测试环境下误识率 FAR ≤0.01%、拒识率 FRR ≤5%。家庭监控为非配合式，熟人槽召回基线定 80% 是工程化打折；精确率 95% 不打折——把陌生人认成熟人是安全漏洞。
- **GA/T 1399.2-2017（公安视频图像分析系统 第2部分：视频图像内容分析及描述技术要求）**：定义了内容分析与结构化描述的功能框架；公开文本不含具体准召数值，工程验收惯例为检出率/准确率 ≥90%。
- 注意：检索确认 **GB/T 42018-2022 是"AI 平台计算资源规范"，与视频智能分析无关**，勿引用错。

### 3.4 厂商与产品公开指标（消费级对标）

- 海康威视 2025 年发布视觉大模型周界摄像机：**误报率较传统方案下降 90% 以上**，可过滤灯光、阴影、小鸟、猫狗、树叶等干扰。
- 消费级门铃/摄像头横评（2026）：Google Nest（端侧 Gemini）人形/包裹/动物/车辆区分"高可靠、测试中零漏报"；Aqara G4 误报 <1 次/天，Ring 平均约 6 次/天——**≤1 次/路/天是消费级第一梯队的误报水位**。
- Nest 支持包裹放下与被取走双事件，证明"包裹存在+取走/放置"在端侧算力上可产品化。

### 3.5 端侧算力约束（Intel Core Ultra 5 + 32GB）对指标的影响

- **内存不是瓶颈**：Qwen3.5-4B INT4 权重约 2.5~3GB，峰值 <6GB；32GB 可同时常驻检测/跟踪小模型 + VLM。
- **吞吐是瓶颈**：OpenVINO 对 NPU 的建议是 ≤4~5B 模型用 INT4 对称量化（group size 128）；Core Ultra NPU 上小模型 INT4 约 6 tok/s（iGPU 通常 15~25 tok/s）。一条 60~80 token 文案生成约 3~10s；每事件 8~16 帧、448~768 短边的视觉编码另需数秒。**单事件端到端 5~15s，适合事件后离线生成，不适合逐秒实时描述。**
- **抽帧率决定行为槽召回上限**：1 fps 抽帧会漏掉 <1s 的瞬时动作。S 级行为召回不能全压在 VLM 上——由小模型在原始帧率触发候选事件，VLM 只做复核+填槽。
- **分辨率决定小目标槽位上限**：包裹品牌 logo、眼镜在门口机位距离下常 <32px——品牌/佩戴槽召回基线低且必须保留"未知"出口。
- **夜间红外是颜色类槽位的硬边界**：红外画面无色彩信息，颜色槽应策略性输出"未知"（保精确率、放弃召回），评测集中红外样本单列统计。

### 3.6 生成式口径的公开实测数据（v1.1 新增，口径①②的定值依据）

**① 自由生成文案的事实性——比判别基准低一个量级：**

- **DREAM-1K**（1000 条视频的细粒度描述基准，AutoDQ 事件级比对）：专为视频描述训练的 SOTA 模型 **Tarsier2-7B 事件级 F1 仅 42%**（recall 48% / precision 35~40%），且是**首个 recall 超过 40% 的模型**；**GPT-4o 仅 39.2%**。即：让最强模型自由描述一段视频，它平均只说出不到一半的关键事件，说出的内容里还有约三成对不上。
- **CAPability**（约 1.1 万图像/视频、12 维度人工标注，precision + hit rate 双指标）：旗舰闭源模型（GPT-4o、Gemini-2.0-flash）自由描述精确率约 **79%**，F1 约 67%；**7B 开源最佳（Qwen2.5-VL-7B）precision 75.5% / recall 48.1% / F1 54.8%**。分维度看：物体类别、场景、OCR 表现好；**物体数量、颜色、空间关系、事件四个维度全体模型"高精确、低召回"**——自由描述时模型只说有把握的、系统性漏说。
- **K̄T（know but cannot tell）现象**：CAPability 专门度量了"问答能答对、自由描述却不提"的比例，证明**同一模型在问答口径和自由生成口径下的召回差距是结构性的**——这正是本方案必须用槽位约束而非自由生成的直接证据。

**② 槽位约束（闭集问答）口径——回到"零样本分类器"水平：**

- 零样本动作识别：InternVL 级别模型在 Kinetics-400/600/700 上 top-1 约 **76.1% / 75.5% / 67.5%**（单帧中心采样；8 帧均匀采样再 +3.3 个点以上）；UCF-101 零样本约 81%。监控闭集 13 类动作词表比 Kinetics 400 类容易，但监控视角/红外/遮挡又抵消部分优势，故 4B 档动作槽约束问答定 65~78%。
- 零样本异常/摔倒判定：VQA 式 GPT-4V 零样本异常检测可达到"距专门训练的 DNN 基线 10% 以内"；训练免调的 VLM 提示方法在 UCF-Crime 上 AUC 89.8%。说明**给定候选事件后让 VLM 做"是/否+选择"复核是可靠的**，但从零发现事件（无小模型触发）不可靠。
- 幻觉控制：POPE/MME-Hall 类基准显示 7B 档模型存在性问答 F1 约 85~88，颜色/计数是幻觉高发维度——约束解码（限定输出词表+JSON schema）可把"词表外幻觉"压到零，剩余错误转化为可度量的分类错误。

**③ 数值推导规则（4B 端侧折扣）**：口径②目标值 = 对应零样本问答水平（7B 档）− 4B 折扣 3~6 点 − 监控画质折扣 0~5 点；口径③目标值 = 专用小模型学术 SOTA − 工程化折扣 3~5 点（判别槽）或采用口径②与小模型融合取高（语义槽）。§1.2 表即按此推得。

---

## 四、逐词槽定值依据（口径③：混合架构）

> 本章数值均为混合架构口径。若某槽在贵方案中实际由纯 VLM 判定，请改用 §1.2 口径②列的区间。

### 4.1 主体词槽（目标检测+人脸库）

人形存在参照 CrowdHuman（密集场景 mAP50 78.4%，家庭遮挡远少于此）与消费级第一梯队产品（Nest 零漏报、Aqara <1 误报/天）定 95→97。熟人槽精确率对标 GB/T 31488 的 FAR 精神从严不放（95%），召回按非配合式场景对 FRR≤5% 打折至 80→88。人群属性参照 Adience 89.5%/UTKFace 94~95% 再打折扣。职业角色本质是"制服+装备"组合属性识别，按 PAR 中档水平设定并限定"制服可见"前提。车辆颜色/类型按 94~95% 学术水平打折。包裹品牌受 logo 小目标+OCR 限制，召回基线最低。

### 4.2 属性词槽（大模型，槽位约束问答）

对标 PAR 三大基准 SOTA 的 P≈88~90/R≈86~89，考虑两点下调：(a) 属性槽由 VLM 以闭集问答判定（口径②），比专用判别模型低 5~10 点；(b) 颜色是公认幻觉敏感维度（CAPability"高精确低召回"四维度之一、MME-Hall 四大幻觉维度之一）。故颜色槽 P85→90/R75→82 并单设幻觉率 ≤5%。体型主观性强，只允许高置信输出。

### 4.3 行为词槽（小模型检测触发+大模型复核）

- **摔倒**：实验室基准 94~99% 的数字不可直接当目标；贴近实战的方案在 P 88~93/R 91~94。召回优先：R 基线 93（触发段召回 ≥97 × VLM 复核保留 ≥96）、P 允许 82 起步。VLM 复核的可靠性由"VQA 式异常判定距专用 DNN 10% 以内"支撑。
- **攀爬/徘徊**：对标实际部署系统（检出率 97.1%/精确率 89.2%）与海康大模型周界"误报降 90%"水位；徘徊是"跟踪时长>阈值"的规则判定，**VLM 从抽帧中无法判定时序徘徊，此槽不允许由大模型独立产出**。
- **取走/放置/按铃**：对应 TAL 任务（学术 SOTA THUMOS14 平均 mAP ≈74%），是全词槽真实难度最高档，召回基线 70。按铃用门铃硬件信号兜底。
- **基础动作**：由检测框+轨迹+越线规则直接计算，不依赖 VLM 语义，按传统视频分析验收惯例（≥90%）设定。

### 4.4 状态词槽（大模型，槽位约束问答）

姿态分类文献总体 91~96%，分姿态差异极大（站 98.3% vs 过渡姿态 75.9%）。站/坐/躺 85→90；蹲/趴/抱着 78→85。"躺着"参与摔倒复核链路时按 S 级召回要求单独考核。

### 4.5 场景词槽（大模型+机位标定）

Places365 Top-1 ~56% 是 365 类开放集数字；本方案 ~15 类闭集且机位固定——**场景应在安装/配置时由 VLM 标定一次+人工确认，运行期只做变化检测**，此时指标本质是配置正确率（95→98）。仅云台/移动机位需逐事件判定（85→90）。场景恰是 VLM 强项（CAPability 中 scene 维度"表现好"组）。

### 4.6 时长词槽（跟踪 ID 累计）

MOT SOTA 的 MOTA/IDF1 ≈ 80 说明断轨不可避免，时长会**系统性低估**。因此：(a) 用分桶+容差口径；(b) 跟踪段间隔 ≤3s 合并同一事件；(c) 时长槽不设独立召回。**此槽严禁由 VLM 产出**——计数/时长是 VLM 公认最弱维度（CAPability 计数维度全体模型低召回）。

---

## 五、推荐架构：小模型+大模型混合出文案（v1.1 新增）

### 5.1 设计原则

1. **发现靠小模型，理解靠大模型**：候选事件的发现（人形/车辆/宠物检测、跌倒姿态触发、越线/徘徊规则）由小模型在高帧率下完成，保召回；VLM 只对已触发的事件做复核与语义填槽，保精确。
2. **大模型只做闭集选择，不做自由陈述**：每个语义槽以"候选项+未知"的闭集问答形式向 VLM 提问，输出受 JSON Schema 约束解码——词表外的值在解码层就不可能出现，幻觉被转化为可度量的分类错误。
3. **文案由模板拼接，不由模型撰写**：最终文案 = 词槽值填入模板（"[时长]内，[属性][主体]在[场景][行为]，[状态]"），文本层不引入任何新事实。DREAM-1K/CAPability 证明自由生成的召回和事实性不可靠，模板化把文案质量完全归结为词槽质量，评测闭环干净。
4. **每槽有唯一权威来源，冲突有仲裁规则**。

### 5.2 词槽归属表（谁产出、谁复核）

| 词槽 | 权威来源 | VLM 角色 | 冲突仲裁 |
|------|---------|---------|---------|
| 人形/车辆/宠物/包裹存在 | 检测小模型（原始帧率） | 复核截图，剔除误检（树影/灯光/海报人像） | VLM 可否决小模型（降误报），不可新增（防幻觉） |
| 熟人/陌生人 | 人脸库小模型 | 不参与身份判定 | 人脸置信度低于阈值一律"陌生人" |
| 人群属性/职业/佩戴/颜色/体型 | VLM 槽位约束问答 | 权威来源 | 多帧投票（≥3 关键帧一致才输出，否则"未知"） |
| 摔倒/攀爬 | 姿态/跌倒小模型触发 | 复核（"图中的人是否倒地？"闭集问答） | 触发+复核双通过才推送；复核否决进低优先级记录 |
| 徘徊/停留/静止/经过/进入/穿越 | 跟踪轨迹+区域规则 | 不参与 | — |
| 取走/放置/按铃 | 前后帧差分（物品增减）+ 门铃传感器 | 对差分结果做语义确认 | 传感器信号 > 视觉 |
| 状态（姿态） | VLM 槽位约束问答（骨架小模型可选加持） | 权威来源 | 与摔倒触发联动时以小模型为准 |
| 场景 | 安装期 VLM 标定+人工确认 | 标定期权威，运行期只做变化检测 | — |
| 时长 | 跟踪 ID 累计（gap≤3s 合并） | 不参与 | — |

### 5.3 推理流程与算力预算（Ultra 5 + 32GB）

```
原始视频流 ──► 检测+跟踪小模型（常驻，5~10fps，NPU，<2GB）
                    │ 触发候选事件（含类别、轨迹、时长）
                    ▼
             关键帧选择（每事件 4~8 帧：首现/最近/动作峰值帧）
                    ▼
             Qwen3.5-4B（INT4，iGPU/NPU，常驻 ~6GB）
             ├─ 复核问答：该事件是否真实？（是/否）
             └─ 逐槽闭集填空：JSON 约束解码，一次前向多槽输出
                    ▼
             规则融合仲裁（§5.2 归属表）──► 词槽 JSON
                    ▼
             模板拼接文案 + 词表后校验 ──► 推送/入库
```

- 小模型常驻 NPU（检测+跟踪+人脸 <10 TOPS 占用），VLM 走 iGPU，互不抢占；32GB 内存下两者常驻无压力。
- 每事件 VLM 只调用一次（多槽合并为一个 JSON 输出，60~120 token），端到端 5~15s，满足"事件后出文案"的产品形态。
- 该流程同时解决抽帧率矛盾：小模型高帧率保瞬时动作召回，VLM 低帧数控算力。

### 5.4 该架构下的评测方式

- 词槽指标按 §1.1 总表验收（口径③）。
- 增加两个链路指标：**VLM 复核保留率**（真实事件被复核误杀的比例 ≤4%）与 **VLM 否决有效率**（被否决的候选中确为误检的比例 ≥80%）。
- 文案层只需抽检模板拼接正确性（语法/槽位缺省处理），不再需要开放式事实核查。

---

## 六、验收评测集与流程建议

1. **评测集构成**（建议 ≥2000 事件）：白天/夜间红外 ≥3:1；门口/室内/车库/庭院机位全覆盖；S 级行为每类 ≥100 正样本（摔倒可用 UR Fall/UP-Fall/CAUCAFall 补充+自采）；负样本（无事件时段）≥总时长 50% 用于误报/天统计。
2. **标注**：按第二章口径做事件级+词槽级双层标注，"无法判定"显式标出；同义词归一化表随词表版本管理。
3. **分层报告**：每个词槽按 白天/夜间、机位类型 分层输出 P/R，避免均值掩盖夜间塌方。
4. **双阶段考核**：模型迭代看词槽 P/R + 幻觉率 + 复核保留率；产品验收看文案可用率 + S 级端到端召回 + 误报次数/路/天。
5. **回归红线**：任何版本迭代，S 级行为召回与熟人精确率不允许下降。
6. **口径声明**：对外承诺指标时必须注明测量口径（§2.4），避免"判别口径的数字被拿去承诺生成口径的效果"。

---

## 七、主要参考来源

**生成式口径（v1.1 新增）**
- CAPability：arXiv 2502.14914《What Is a Good Caption? A Comprehensive Visual Caption Benchmark...》（NeurIPS 2025 D&B；7B 最佳 P 75.5/R 48.1/F1 54.8；颜色/计数/事件维度高精确低召回；K̄T 指标）
- DREAM-1K / AutoDQ：arXiv 2407.00634《Tarsier: Recipes for Training and Evaluating Large Video Description Models》；arXiv 2501.07888《Tarsier2》（Tarsier2-7B 事件级 F1 42%，GPT-4o 39.2%）
- 零样本动作识别：arXiv 2312.14238（InternVL，K400 zero-shot 76.1%）；arXiv 2410.21113《Zero-Shot Action Recognition in Surveillance Videos》
- VLM 异常判定：arXiv 2311.02612（GPT-4V-AD，距 DNN 基线 10% 以内）；WACV 2026 ASK-HINT（UCF-Crime AUC 89.83，training-free）
- 幻觉基准：POPE、MME-Hall（存在性/颜色/计数/位置四维度）

**判别式口径**
- PAR：arXiv 2408.09720（LLM-PAR）· 跌倒：arXiv 2309.07154、arXiv 2501.01985、MDPI Appl. Sci. 13(12):6916 · TAL：arXiv 2407.13078（THUMOS14 74.2）· 姿态：ACM ICMSIE 2020、PLOS One 2024 · 场景：CSAILVision/places365 · 人脸：WebFace260M、NIST FRTE FIVE · 年龄：Information 17(6):567、Adience · 车辆颜色：arXiv 2408.11589 · 跟踪：arXiv 2206.14651（BoT-SORT）· 行人检测：MDPI Electronics 13(1):236 · 门铃包裹：arXiv 2305.07812

**模型与算力**
- Qwen3-VL 技术报告：arXiv 2511.21631 · Qwen3.5 发布信息（2026-03，4B MLVU 76.2）· OpenVINO GenAI on NPU 文档与 Model Hub 实测

**标准与产品**
- GB/T 31488-2015/2022 · GA/T 1399.1/1399.2-2017 · 海康视觉大模型周界摄像机报道（2025-03）· CNN Underscored / Security.org 2026 门铃横评
