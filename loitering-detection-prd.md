# AI 产品需求文档（PRD）：监控中心 App —— 熟人/陌生人识别与徘徊事件检测（端侧）

| 项目 | 内容 |
| --- | --- |
| 文档版本 | V1.0 |
| 产品/功能名 | 监控中心 App · 人员身份与徘徊事件智能检测 |
| 文档类型 | AI 产品需求文档（含词槽准召指标定义） |
| 目标平台 | 端侧一体机 / 边缘盒子（Intel Core Ultra 5，32GB 内存，无云端算力依赖） |
| 撰写日期 | 2026-07-17 |
| 状态 | 评审稿 |

> **前置说明**：原需求中"词槽如上图"所指的图片未随任务附上。本文档第 6 章根据需求描述与典型监控事件结构，自行完整定义了事件词槽表（人员身份、行为类型、区域、时长等）；若与原图存在出入，请以评审时对齐后的槽位表为准，指标定义方法不受影响。

---

## 1. 背景与产品目标

### 1.1 背景

家庭/园区/商铺监控场景中，用户对"有人出现"这类原始告警已经严重疲劳：风吹草动、快递路过、家人回家都会触发推送。用户真正关心的是**三类有语义的人员事件**：

1. **熟人出现**（家人、员工、常客——通常只需记录，不需打扰）；
2. **陌生人出现**（未注册人员进入关注区域——需要提示）；
3. **有人徘徊**（同一人在区域内停留过久、来回走动、行迹可疑——高优先级告警）。

其中"徘徊"是安防场景公认的高价值异常行为：踩点、尾随、蹲守等行为的共同外显特征就是"长时间驻留 + 来回走动"。学术界（i-LIDS、UCF-Crime、Avenue 等基准）与商用安防厂商（海康、大华、Axis 等的 Smart Event / 徘徊侦测功能）都将其列为标准事件类型。

### 1.2 产品目标

- 在**纯端侧算力**（Intel Core Ultra 5 + 32GB RAM）上，实时完成：人形检测 → 多目标跟踪 → 人脸识别（熟人/陌生人）→ ReID/跨镜关联 → 徘徊行为判定 → 事件文字描述生成（Qwen3.5-4B）。
- 支持用户**圈定区域**，只对圈定区域内的行为做徘徊判定。
- 徘徊**作为独立事件类型上报监控中心**，按有无人脸细分为"有人徘徊"与"陌生人徘徊"两类。
- 事件推送做到"**少而准**"：徘徊事件级精确率 ≥ 90%，单路摄像头误报 ≤ 1 次/天（详见第 7 章指标）。
- 全流程数据不出端，满足人脸数据合规要求（GB/T 41819-2022 等）。

### 1.3 成功指标（北极星）

| 指标 | 目标 |
| --- | --- |
| 徘徊事件推送精确率（用户视角"报得对"） | ≥ 90% |
| 徘徊事件召回率（实际徘徊被报出） | ≥ 85% |
| 单路摄像头徘徊误报 | 白天 ≤ 1 次/天；夜晚红外 ≤ 2 次/天（默认灵敏度，见 7.5） |
| 事件端到端延迟（行为满足条件 → 收到推送） | ≤ 5s |
| 用户告警关闭率（收到告警后关闭该功能的比例） | ≤ 5% |

---

## 2. 名词定义

| 术语 | 定义 |
| --- | --- |
| 熟人 | 已在 App 人员库中注册（录入人脸）的人员，含"家人/员工/常客"等用户自定义标签 |
| 陌生人 | 出现在画面中、人脸比对未命中人员库（或无法完成比对但 ReID 亦无法关联到熟人轨迹）的人员 |
| 徘徊 | 在监控画面（或圈定区域）内**检测到有人出现后即开始计时**，同一身份 ID 累计出现时长 ≥ T（默认 30s，建议后续业务开放给用户设置）即触发徘徊逻辑（精确规则见 4.3） |
| 有人徘徊 | 触发徘徊且**无可用人脸**（背对/过远/遮挡，ReID 亦未关联到熟人）；推送文案："有人在区域内徘徊" |
| 陌生人徘徊 | 触发徘徊且**有人脸、比对为陌生人**；推送文案："有陌生人在区域内徘徊" |
| 圈定区域 | 用户在画面上手绘的多边形区域；未圈定时默认全画面为判定区域 |
| 跟踪 ID（Track ID） | 单摄像头内多目标跟踪器分配的目标编号 |
| 跨镜 ID（Global ID） | 通过 ReID 特征在多摄像头之间关联后的全局人员编号 |
| 词槽（Slot） | 一条结构化事件中需要被模型填充的字段，如 `person_identity`、`behavior_type`、`zone_id`、`duration` 等 |
| 事件 | 一次完整的、可推送给用户的结构化告警记录（含视频片段、截图、文字描述、词槽值） |

---

## 3. 用户与场景

### 3.1 目标用户

- **家庭用户**：门口、庭院、楼道摄像头，关心陌生人徘徊（踩点/蹲守）。
- **小微商户/园区物业**：店门口、仓库、围墙周界，关心非营业时间驻留、围墙外徘徊。
- **监控中心值班员（B 端）**：多路画面，需要事件流代替人工盯屏。

### 3.2 核心用户故事

1. 作为家庭用户，当**陌生人在我家门口圈定区域内徘徊超过 30 秒**，我要立刻收到推送，附带视频片段和一句话描述（如"一名陌生男子在门口区域来回走动约 1 分钟"）。
2. 作为家庭用户，**家人（熟人）在门口停留**不应打扰我，只需要记录在时间线里。
3. 作为商户，我要能**为每路摄像头分别圈定 1~N 个区域**，并为每个区域独立设置徘徊时长阈值和布防时间段（如仅 22:00–6:00 布防）。
4. 作为值班员，我要在事件列表里按"熟人/陌生人/徘徊"筛选，点开即看到目标的跨镜轨迹（他从 A 门走到 B 门）。
5. 作为用户，我误报了要能一键反馈"报错了"，系统据此调整该点位灵敏度。

### 3.3 典型正/负样本场景（用于测试集构建）

| 类别 | 场景举例 |
| --- | --- |
| 应报（正样本） | 陌生人在门口静站 40s；有人沿围墙来回走 3 趟；有人在车库门口蹲下又起身反复；快递员放下包裹后在门口逗留 2 分钟拍照张望 |
| 不应报（负样本/边界） | 快递员 15s 放件即走（直线路过）；家人（熟人）在门口打电话 2 分钟（应记录"熟人驻留"，默认不告警，可配置）；邻居遛狗每次穿过画面 5s 但一晚经过 10 次（每次独立、不累计）；环卫工在区域内工作（可通过熟人库/工装白名单消除）；树影/雨雪/宠物/车灯（不产生人形 ID，不进入行为判定） |

---

## 4. 功能需求与场景边界定义

### 4.1 范围（In Scope / Out of Scope）

**本期做（In）**
- 单摄像头内：人形检测、跟踪、熟人/陌生人识别、区域内徘徊判定、事件生成与推送。
- 多摄像头（同一 App 账号下 ≤ 8 路）：基于 ReID 的跨镜 ID 关联，跨镜轨迹展示；**跨镜徘徊（在多个相邻摄像头覆盖的同一物理区域内累计驻留）作为进阶开关，默认关闭**。
- 用户圈定多边形区域（每路 ≤ 4 个），每区域独立配置：徘徊时长阈值 T（10s–300s，默认 30s）、灵敏度（高/中/低）、布防时间段、告警对象（仅陌生人 / 所有人）。
- Qwen3.5-4B 端侧生成事件文字描述与结构化词槽填充。

**本期不做（Out）**
- 车辆、宠物、非人目标的徘徊判定。
- 打架、跌倒、遗留物等其他异常行为（架构预留事件类型字段）。
- 云端二次识别/云端存储人脸特征（合规红线：人脸特征只存端侧）。
- 人群（≥10 人同屏密集场景，如商场大厅）的逐人徘徊判定——超出端侧算力与 ReID 可靠性边界，此类点位提示用户"该场景仅支持区域入侵统计"。
- 夜间无任何补光（照度 < 1 lux 且无红外）画面的身份识别——只输出"有人徘徊（身份未知）"。

### 4.2 场景边界条件（必须写进验收口径）

| 维度 | 边界定义 |
| --- | --- |
| 目标尺寸 | 人形高度 ≥ 画面高度 6%（1080p 下约 ≥ 65px）才进入行为判定；人脸瞳距 ≥ 30px 才进入人脸识别，否则身份回退为 ReID/未知 |
| 光照 | 白天/夜晚红外/低照度三种模式分别验收；红外模式下不承诺熟人识别准确率达标，身份可降级为"未知"；夜晚红外事件级指标按 7.5 的规则放宽评估（缺省降 5 个百分点） |
| 遮挡 | 目标可见面积 < 40% 持续 > 3s 时允许跟踪中断；中断 ≤ 10s 内由 ReID 重关联并**续算驻留时长** |
| 同屏人数 | ≤ 10 人承诺指标；> 10 人降级为区域级统计 |
| 天气 | 雨、雪、大雾场景只承诺检测与徘徊判定指标（放宽 5pt），不承诺人脸指标 |
| 摄像头 | 固定枪机/半球机；云台转动期间暂停判定并清空该路计时器 |
| 圈定区域边界抖动 | 目标脚点（检测框底边中点）为区域归属判定点；在区域边界 ±5% 画幅内的进出抖动做 2s 去抖，不重置计时 |

### 4.3 徘徊行为的精确定义（判定状态机）

**基础触发条件（业务定义，本期触发口径）**：在监控画面（或用户圈定区域）内**检测到有人出现后即开始计时**；同一身份 ID 累计出现时长达到 **T（默认 30s）** 即触发徘徊逻辑，生成徘徊事件。阈值 T 本期固定为 30s，**建议后续业务开放给用户设置**（范围 10s–300s）。

**身份分支与事件判定（触发后执行）**：

| 人脸情况 | 事件判定 | 推送文案（固定口径） |
| --- | --- | --- |
| 无人脸（背对/过远/遮挡，且 ReID 未关联到熟人） | **有人徘徊** | "有人在区域内徘徊" |
| 有人脸，且比对为陌生人 | **陌生人徘徊** | "有陌生人在区域内徘徊" |
| 有人脸，且比对为熟人 | 熟人驻留（默认仅记录不推送，可配置） | "（人员标签）在区域内停留" |

**判定主体**：单个身份 ID（优先跨镜统一 ID，退化为单镜跟踪 ID）。
**判定区域**：某个圈定区域（未圈定则全画面）。
**判定点**：目标检测框底边中点（脚点）落在圈定区域多边形内。

**出现时长累计规则**
1. 脚点进入圈定区域，启动该（ID, 圈定区域）计时器；
2. 脚点离开圈定区域后 **≤ 10s 内**（离开缓冲窗）同一 ID 再次进入，计时**续算**且累计"折返次数 +1"；> 10s 再进入则计时清零重新开始；
3. 跟踪 ID 丢失后 ≤ 10s 内被 ReID 重关联为同一人，计时续算；否则按新目标处理。

**运动模式子类（仅用于事件标注与描述增强，不作为触发门槛）**：
- **静止驻留型**：期间目标净位移 < 自身身高的 1.5 倍（"基本没动"）；
- **来回走动型**：`路径总长 / 净位移 ≥ 3`（折返比）或方向反转次数 ≥ 2 次；
- **移动停留型**：不满足以上两类、在区域内持续活动。

**抑制与排除规则**：
- 身份为熟人且该区域配置为"仅陌生人告警"→ 记录不推送；
- 同一（ID, 圈定区域）触发徘徊后进入 **5 分钟事件抑制期**，期间持续徘徊只更新原事件时长，不重复推送；
- **缓慢直线路过排除**（可选开关，默认关闭）：累计出现 ≥ T 但轨迹为单向近直线穿越（折返比 < 1.5 且持续朝一个方向）时判为"缓慢路过"不报徘徊；若上线后此类误报偏高，可按点位开启。

### 4.4 圈定区域交互需求

1. 支持在实时画面/截图上手绘**凸或凹多边形**（3–10 个顶点），支持拖拽顶点微调；每路摄像头最多 4 个圈定区域，可命名（"大门""围墙西侧"）。
2. 每个圈定区域独立配置：徘徊阈值 T、灵敏度档位、布防时间表、告警身份范围（所有人/仅陌生人）、事件抑制期时长。
3. 提供"默认全画面"一键模式；圈定区域修改即时生效，修改时清空该区域在途计时器。
4. 画面中实时叠加圈定区域半透明高亮与目标框（熟人绿色/陌生人橙色/徘徊中红色 + 已出现秒数角标），用于用户调试灵敏度。
5. 圈定区域以摄像头分辨率归一化坐标存储，分辨率切换不失效。

### 4.5 事件呈现与推送

- **徘徊作为独立事件类型上报监控中心**：不并入"陌生人出现/熟人到访"等既有事件；同一目标先触发"陌生人出现"、后满足徘徊条件时，徘徊**单独生成一条事件**上报（原事件保留），且徘徊事件优先级更高。
- 徘徊推送文案为固定口径：无人脸 →"有人在区域内徘徊"；有人脸且为陌生人 →"有陌生人在区域内徘徊"。LLM 生成的详细描述作为事件卡片的补充说明，不替换固定推送文案。
- 事件卡片：封面截图（目标高亮）+ 一句话描述（LLM 生成）+ 词槽结构化字段 + 前后各 5s 的视频片段（事件全程 ≤ 60s 截断）。
- 时间线支持按事件类型（熟人到访/陌生人出现/有人徘徊/陌生人徘徊）、按摄像头、按圈定区域、按人员筛选。
- 徘徊事件推送等级默认"强提醒"；熟人事件默认静默记录。
- 用户可对每条事件反馈"准确/误报/漏报补录"，反馈数据用于端侧阈值自适应与后续模型迭代评估。

---

## 5. 技术方案概述（AI Pipeline）

```
视频流(1080p) ─► ① 人形检测(YOLO 系列, NPU/iGPU, OpenVINO)
                     │
                     ▼
               ② 多目标跟踪(ByteTrack/BoT-SORT 类, CPU)
                     │                        
                     ├──► ③ 人脸检测+识别(瞳距≥30px 时, 1:N 比对熟人库)
                     │
                     ├──► ④ ReID 特征提取(人脸不可用时身份维持/跨镜关联)
                     │
                     ▼
               ⑤ 区域归属 + 徘徊状态机(纯几何/规则, CPU, 确定性逻辑)
                     │
                     ▼
               ⑥ 事件组装 → Qwen3.5-4B 生成文字描述 + 词槽校验(结构化输出)
                     │
                     ▼
               推送 / 时间线 / 本地存储
```

**关键设计原则**
- **⑤ 徘徊判定用规则状态机而非黑盒模型**：出现时长阈值是可解释、可开放给用户设置的量；模型只负责"看得准"（①–④），规则负责"判得稳"。这也是学术界主流做法（YOLO + DeepSORT/跟踪 + 时长/位移阈值判定，见 IEEE 2020 多摄像头徘徊检测、MDPI JSAN 2023 时空信息徘徊检测等论文）。
- **⑥ LLM 不参与徘徊判定**，只做事件的自然语言描述与词槽规范化填充（JSON Schema 约束解码），从机制上杜绝 LLM 幻觉影响告警准召。
- 人脸与 ReID 双通道互补：脸部可用时以人脸定身份；背对/远距/戴口罩时以 ReID 维持身份连续性。

---

## 6. 词槽定义（Slot Schema）

> 原图缺失，以下为根据需求推导的完整词槽表（评审时对齐）。

一条徘徊/身份事件的结构化输出：

| # | 词槽 | 类型/取值 | 填充来源 | 必填 |
| --- | --- | --- | --- | --- |
| S1 | `event_type` | 枚举：`loitering_person`（有人徘徊）/ `loitering_stranger`（陌生人徘徊）/ `stranger_appear` / `known_person_appear`；徘徊为**独立事件类型，单独上报监控中心** | 规则引擎 + 人脸比对 | 是 |
| S2 | `person_identity` | 枚举：`known`（+人员库 person_id 与姓名标签）/ `stranger` / `unknown`（无法判定） | 人脸 1:N + ReID | 是 |
| S3 | `person_count` | 整数（本事件涉及人数） | 检测+跟踪 | 是 |
| S4 | `zone_id` / `zone_name` | 圈定区域编号与用户命名；全画面时为 `full_frame` | 几何判定 | 是 |
| S5 | `camera_id` | 摄像头编号；跨镜时为有序列表 | 系统 | 是 |
| S6 | `global_person_id` | 跨镜统一人员 ID | ReID 关联 | 徘徊事件必填 |
| S7 | `behavior_subtype` | 枚举：`stationary`（静止驻留）/ `pacing`（来回走动）/ `moving`（移动停留）；仅作标注与描述增强，**不参与触发判定** | 徘徊状态机 | 徘徊事件必填 |
| S8 | `start_time` / `end_time` / `duration_sec` | 时间戳/秒 | 状态机 | 是 |
| S9 | `loiter_metrics` | 折返次数、净位移/路径比 | 状态机 | 徘徊事件必填 |
| S10 | `snapshot` / `clip` | 截图与片段引用 | 系统 | 是 |
| S11 | `description` | 自然语言一句话描述（≤ 40 字）；徘徊推送标题用固定文案（见 4.5），LLM 描述作卡片补充 | Qwen3.5-4B | 是 |
| S12 | `confidence` | 事件综合置信度 0–1 | 各模型分数融合 | 是 |

其中 **S1、S2 是决定"报不报、报什么"的核心语义槽**（徘徊触发后由 S2 的人脸结果决定"有人徘徊"还是"陌生人徘徊"），S7 为标注槽，S4/S8 是限定槽，S11 为呈现槽。第 7 章逐槽给出准召指标。

---

## 7. 各词槽准召指标定义（核心章节）

### 7.1 指标口径先行

- **帧级指标**（检测/识别环节内部指标）：以抽检标注帧为单位算 P/R。
- **事件级指标**（用户可感知，验收主口径）：一次告警为一个样本。预测事件与真值事件的**时间 IoU ≥ 0.3 且身份/区域槽一致**记为 TP。
  - 事件精确率 P = 报出的事件中报对的比例（用户视角"不烦"）；
  - 事件召回率 R = 实际发生的事件中被报出的比例（用户视角"不漏"）；
  - 辅以 **FAR（False Alarms per camera per day，单路日均误报数）**——工业界（i-LIDS 等测评体系）证明纯 P/R 不足以约束长时间空场景的误报体验，必须加每日误报上限。
- 每个槽给出 **P0（发布底线）** 与 **P1（体验目标）** 两档。

### 7.2 指标推导依据（公开基准 → 产品指标的换算逻辑)

产品指标 ≠ 论文 SOTA。设定原则：取公开基准上**轻量级（端侧可跑）模型**的成绩，按"实际监控画质低于学术数据集"打 3–10pt 折扣，再验证该指标能否支撑北极星 FAR 目标。关键公开数据：

| 环节 | 公开基准结果（依据） |
| --- | --- |
| 人形检测 | CrowdHuman（密集行人，均 23 人/图）：YOLOv11-s ≈ 65.8% mAP@0.5、YOLOv11-l ≈ 74.9%；轻量改进模型（MSCD-YOLO 等）较 YOLOv8n 提升 4–5pt。监控场景通常稀疏（≤10 人），同模型 mAP 显著高于 CrowdHuman 成绩 |
| 多目标跟踪 | MOT17：ByteTrack MOTA 80.3 / IDF1 77.3；BoT-SORT MOTA 80.5 / IDF1 80.2（MOT20 密集场景约低 3–5pt） |
| 人脸识别 | IJB-C（含监控级低质图像）TAR@FAR=1e-4：ArcFace(R100) 95.6%，AdaFace 97.4%；NIST FRTE/FRVT 1:N 头部算法 FNIR 0.0017@FPIR=0.001（证件照质量，监控画质需大幅折扣）；国内有 GB/T 38671、GA/T 1093 等标准对误识率/拒识率提出分级要求 |
| ReID | Market-1501：SOTA rank-1 ≈ 95.4% / mAP ≈ 92.8%；跨 15 镜头的 MSMT17：rank-1 ≈ 86.4% / mAP ≈ 74.6%——说明**跨镜、多视角下 ReID 显著变难**，端侧轻量模型还要再降 |
| 徘徊行为 | 学术论文（YOLOv3+DeepSORT 时长/位移阈值法、时空特征法、运动状态自适应法）在 UCF-Crime/Avenue/自建集上典型报告 accuracy/P/R 在 85–95% 区间；商用系统以 FAR/日为主要承诺口径 |
| LLM 结构化 | Qwen3-4B：MMLU-Redux 83.7、指令遵循经调优后每条准则通过率 ≈ 73%→ 结构化槽填充必须用受约束解码（JSON Schema）兜底而非裸生成；Qwen3.5-4B（2026.02 发布，Apache 2.0，4-bit 约 2.5–3GB，256K 上下文，支持图文输入）能力更强，简单模板化描述任务可达到很高槽准确率 |

### 7.3 词槽准召指标总表

**A. 感知环节（帧级，内部质量门禁）**

| 环节/槽 | 指标 | P0（底线） | P1（目标） | 测试集口径 |
| --- | --- | --- | --- | --- |
| 人形检测（S3 基础） | 帧级 Precision / Recall（IoU≥0.5，目标高≥6%画高） | P≥95% / R≥90% | P≥97% / R≥95% | 自建监控测试集（日/夜/雨各 ≥5000 帧）+ CrowdHuman 子集回归 |
| 跟踪连续性（S6/S8 基础） | IDF1 / ID switch 率 | IDF1≥70%，IDsw≤1 次/分钟/目标 | IDF1≥78% | MOT17 私有检测协议 + 自建 30s 长时驻留片段 |
| 人脸识别-熟人命中（S2=`known`） | 1:N TPIR（正确识出熟人）@FPIR≤1%，库容≤100 人，瞳距≥30px | ≥90%（白天）/ ≥80%（红外） | ≥95% / ≥88% | 自建 50 户家庭库 + IJB-C 低质子集回归 |
| 人脸识别-陌生人拒识（S2=`stranger`） | 陌生人被误认成熟人的比例（FPIR） | ≤1% | ≤0.3% | 同上，陌生人 ≥1000 人次 |
| ReID 单镜重关联 | 中断≤10s 重关联 rank-1 | ≥90% | ≥95% | 自建遮挡/出画回归片段 |
| ReID 跨镜关联（S6） | 跨镜 rank-1（2–8 路，视角差≤90°） | ≥80% | ≥88% | 自建多机位集 + MSMT17 回归（参考其 rank-1 86% 的难度水位） |

**B. 语义槽（事件级，验收主口径）**

| 词槽 | 指标 | P0（底线） | P1（目标） | 备注 |
| --- | --- | --- | --- | --- |
| S1 `event_type`=徘徊（含有人徘徊/陌生人徘徊） | 事件 P / R + FAR（白天口径） | **P≥90% / R≥85%，FAR≤1 次/路/天** | P≥95% / R≥90%，FAR≤0.5 次/路/天 | 北极星；夜晚红外按 7.5 放宽评估；时长误差另行约束（见 S8） |
| S1 徘徊身份细分（有人徘徊 vs 陌生人徘徊判对） | 细分正确率 | ≥90% | ≥95% | 无可用人脸时判"有人徘徊"视为正确；熟人被误报进徘徊告警单独统计 ≤2% |
| S1 =`stranger_appear` | 事件 P / R | P≥88% / R≥90% | P≥93% / R≥95% | 陌生人宁可多报（R 优先） |
| S1 =`known_person_appear` | 事件 P / R | P≥95% / R≥85% | P≥97% / R≥92% | 熟人误标陌生人会引发误告警，P 优先 |
| S2 `person_identity` | 槽准确率（在事件被报出的前提下身份判对） | ≥92% | ≥96% | 熟人→陌生人、陌生人→熟人双向混淆分开统计；红外场景允许输出 `unknown` 且 `unknown` 占比≤20% |
| S4 `zone_id` | 槽准确率 | ≥98% | ≥99.5% | 几何计算为主，误差来自脚点抖动 |
| S6 `global_person_id` | 跨镜同一人合并正确率 | ≥85% | ≥92% | 错误合并（两人并成一人）单独统计 ≤3% |
| S7 `behavior_subtype` | 三子类分类准确率（事件为真徘徊前提下） | ≥85% | ≥92% | 子类错不影响告警正确性，属体验槽 |
| S8 `duration_sec` | 时长绝对误差 | ≤3s（90 分位） | ≤2s | 含 ID 中断续算场景 |
| S11 `description`（LLM） | 描述关键要素正确率（身份/人数/区域/行为四要素均对） | ≥95% | ≥98% | 受约束解码 + 模板兜底；**幻觉率（描述出画面不存在的要素）≤1%** |
| S11 生成延迟 | 事件触发→描述生成完成 | ≤2s | ≤1s | Qwen3.5-4B INT4，NPU/iGPU 推理 |
| S12 `confidence` | 校准误差 ECE | ≤0.1 | ≤0.05 | 供灵敏度档位映射使用 |

**C. 灵敏度档位与指标的关系**

| 档位 | 设计取向 | 预期 P/R 移动 |
| --- | --- | --- |
| 高灵敏 | R 优先（周界、夜间布防） | R +5pt，FAR 放宽到 ≤3 次/路/天 |
| 中（默认） | 平衡 | 上表 P0/P1 |
| 低灵敏 | P 优先（人流较多的店门口） | P +3pt，R 允许 -8pt |

### 7.4 指标设定的三点说明（评审重点）

1. **徘徊事件 P≥90%/R≥85% 是"规则判定"红利下的合理值**：出现时长是确定性计算，事件错误几乎全部来自上游（检测漏检 → 计时断、ID switch → 时长算错、身份错 → 细分报错）。按 A 表帧级指标做误差传播估算，30s 窗口内维持同一 ID 的成功率约 92–95%，与论文报告的 85–95% 区间一致，故 P0 取 90/85 可达且有余量。
2. **FAR/日必须与 P/R 并列**：一路夜间空场景 12h 无事件，P/R 无法体现"每晚被树影吵醒 3 次"的体验崩坏；i-LIDS 等标准测评正是以告警/误报事件计数为核心口径。
3. **人脸指标在监控画质下必须与 NIST 证件照成绩脱钩**：NIST FRTE 1:N 头部算法 FNIR≈0.2% 是受控画质；IJB-C（含监控帧）TAR@FAR=1e-4 顶尖也仅 97.4%，且为大模型非端侧。端侧轻量模型 + 红外 + 大角度俯拍，P0 定 90%（白天）/80%（红外）是有公开依据支撑的工程可达值，不应对齐门禁机指标。

### 7.5 夜晚红外场景的指标评估规则

夜晚红外场景**接受误报略高于白天**，按以下规则评估：

- **缺省口径（降 5% 评估）**：事件级精确率/召回率在白天 P0/P1 基础上**放宽 5 个百分点**。例如徘徊事件白天 P0 为 P≥90% / R≥85%，夜晚红外按 **P≥85% / R≥80%** 评估；FAR 放宽至 **≤2 次/路/天**。
- **以算法侧实测建议为准**：上述"降 5%"是算法侧未给出建议时的缺省评估口径；若算法侧在红外测试集上给出实测建议值（无论更优或更保守），以算法侧建议更新本表并重新评审。
- **身份槽放宽**：夜晚红外下身份允许更高比例输出"未知"（`unknown` 占比 ≤35%，白天 ≤20%）；身份未知不影响徘徊事件本身的触发与上报（按"有人徘徊"口径上报）。
- **统计口径**：夜晚红外指标单独分列统计，不与白天混合平均；验收测试集中夜晚红外样本占比不低于 30%（见第 9 章配比）。

---

## 8. 端侧算力与资源预算（Intel Core Ultra 5 / 32GB RAM）

### 8.1 硬件能力盘点

- Intel Core Ultra 5（Meteor Lake / Arrow Lake 档位）：NPU 约 10–13 TOPS（INT8），iGPU 与 CPU 合计平台总算力约 30–40 TOPS；200V 系列平台标称最高可达 120 TOPS（若采用该档位余量更大）。
- 实测参考：OpenVINO 下 YOLO 导出可获得最高 3× CPU 加速；Core Ultra 平台 iGPU 跑 YOLO-nano 级模型单帧约 4ms，NPU 约 12–20ms（新驱动 ~45ms→ 优化后）。NPU 适合长时间持续负载且能效比高（较上代最高 2.56× 性能/瓦）。
- 内存 32GB：Qwen3.5-4B INT4 约 2.5–3GB 常驻；全部视觉模型（检测 + 人脸 + ReID，INT8）合计 < 1GB；视频缓冲与系统预留后余量充足。

### 8.2 算力分配方案（4 路 1080p 为设计基准，8 路为上限降级）

| 任务 | 部署目标 | 频率 | 预估负载 |
| --- | --- | --- | --- |
| 人形检测（YOLO-n/s，640 输入，INT8） | NPU（持续负载） | 每路 10 fps | 4 路 ≈ 40 次/s |
| 跟踪（ByteTrack 类，纯 CPU 运算） | CPU | 与检测同步 | 低 |
| 人脸检测+识别 | iGPU | 事件相关目标按需触发，≤5 次/s | 低 |
| ReID 特征提取 | iGPU | 目标进入圈定区域 / ID 待重关联时触发 | 低 |
| 徘徊状态机 | CPU | 每帧 | 可忽略 |
| Qwen3.5-4B INT4 描述生成 | iGPU（或 NPU 分时） | 每事件 1 次，输出 ≤ 80 token | 突发 ≤2s |

**降级策略**：>4 路时检测降到 6–8 fps；同屏 >10 人时人脸/ReID 只处理圈定区域内目标；LLM 生成排队不阻塞告警推送（先推固定文案，描述生成后更新卡片）。

---

## 9. 验收测试方案

1. **自建端到端测试集（验收主依据）**：≥50 个点位、≥200 小时录像；徘徊正样本 ≥300 段（静止/来回/移动三子类各 ≥80，有人徘徊与陌生人徘徊两类身份均覆盖），负样本含 3.3 节全部边界场景；日/夜/雨雪按 5:3:2 配比；每段标注真值词槽（S1–S9）。
2. **公开集回归（防退化门禁）**：CrowdHuman-val（检测）、MOT17（跟踪）、IJB-C 低质子集（人脸）、MSMT17（ReID），每次模型更新跑分不得低于上一版本 1pt。
3. **长稳测试**：8 路满载连续 7×24h，无内存泄漏、帧率达标、FAR 统计达标。
4. **灰度**：先向 5% 用户开放"仅记录不推送"影子模式 2 周，用真实反馈标定 FAR 与阈值，再放量。

---

## 10. 合规、隐私与风险

| 风险 | 对策 |
| --- | --- |
| 人脸数据合规（GB/T 41819-2022 人脸识别数据安全要求等） | 人脸特征只存端侧并加密；提供一键删除人员库；陌生人截图默认 7 天自动清理；App 首次启用需用户明示授权 |
| 徘徊误报引发邻里纠纷（把正常等人报成可疑） | 文案中性化（"有人在门口停留 1 分钟"而非"可疑人员"）；不做任何危险性/意图推断 |
| 熟人误标陌生人（红外/口罩） | 身份不确定时输出 `unknown` 而非强行二分；推送文案区分"陌生人"与"身份未确认" |
| ID switch 导致时长虚增 | 离开缓冲窗与 ReID 重关联双保险；时长 90 分位误差 ≤3s 列入验收 |
| LLM 描述幻觉 | 受约束解码 + 词槽回填校验；描述与词槽冲突时以词槽为准并回退模板文案 |

---

## 11. 里程碑建议

| 阶段 | 内容 | 出口标准 |
| --- | --- | --- |
| M1（4 周） | 单路：检测+跟踪+圈定区域徘徊状态机 | 徘徊事件 P≥85%/R≥80%（自建集） |
| M2（4 周） | 人脸熟人/陌生人 + 事件卡片 + Qwen3.5-4B 描述 | S2 槽准确率 ≥90%，描述要素正确率 ≥93% |
| M3（4 周） | 多路 + ReID 跨镜 + 降级策略 + 长稳 | 全部 P0 指标达标，8 路 7×24h 长稳通过 |
| M4（2 周） | 影子模式灰度与阈值标定 | FAR ≤1 次/路/天（真实用户数据） |

---

## 12. 参考资料（公开基准与报道）

- 行人检测：[CrowdHuman: A Benchmark for Detecting Human in a Crowd](https://www.researchgate.net/publication/324886881_CrowdHuman_A_Benchmark_for_Detecting_Human_in_a_Crowd)；[MSCD-YOLO（MDPI Sensors 2025）](https://www.mdpi.com/1424-8220/25/2/438)；[GR-YOLO 密集行人检测](https://pmc.ncbi.nlm.nih.gov/articles/PMC11281216/)
- 多目标跟踪：[ByteTrack (ECCV 2022)](https://arxiv.org/abs/2110.06864)；[BoT-SORT](https://arxiv.org/pdf/2206.14651)；[Deep OC-SORT](https://arxiv.org/pdf/2302.11813)
- 人脸识别：[NIST FRVT/FRTE 1:N（NEC FNIR 0.0017@FPIR=0.001）](https://www.necam.com/en_US/biometrics/NISTValidation/FingerprintFacial/index.html)；[AdaFace: Quality Adaptive Margin (CVPR 2022)](https://ar5iv.labs.arxiv.org/html/2204.00964)；[ArcFace](https://arxiv.org/pdf/1801.07698)；[Surveillance Face Recognition Challenge (QMUL-SurvFace)](https://arxiv.org/pdf/1804.09691)；国内标准：[GB/T 38671-2020 远程人脸识别系统技术要求](https://www.gcs66.com/document_detail/102055.html)、[GA/T 1093-2023 出入口控制人脸识别技术要求](https://ndls.org.cn/standard/detail/be337ecea1141eb2bd5765e58a5b289f)、[GB/T 41819-2022 人脸识别数据安全要求](https://www.antpedia.com/standard/1841256335-1.html)
- ReID：[Market-1501 SOTA 汇总](https://zheng-lab-anu.github.io/Project/state_of_the_art_market1501.html)；[Neighbor-Based Feature and Index Enhancement for ReID (CVPRW 2025)](https://arxiv.org/pdf/2504.11798)
- 徘徊检测：[Deep Learning Based Loitering Detection System Using Multi-Camera Video Surveillance Network (IEEE 2020)](https://ieeexplore.ieee.org/document/9002479/)；[Loitering Detection Using Spatial-Temporal Information (MDPI JSAN 2023)](https://www.mdpi.com/2224-2708/12/1/9)；[Adaptive loitering anomaly detection based on motion states (Springer MTA 2024)](https://link.springer.com/article/10.1007/s11042-024-20296-7)；[i-LIDS: A Standard for Testing Video Based Detection Systems](https://www.researchgate.net/publication/284353547_Imagery_Library_for_Intelligent_Detection_Systems_i-LIDS_A_Standard_for_Testing_Video_Based_Detection_Systems)
- 端侧模型与算力：[Qwen3 Technical Report](https://arxiv.org/pdf/2505.09388)；[Qwen/Qwen3.5-4B · Hugging Face](https://huggingface.co/Qwen/Qwen3.5-4B)；[Qwen3.5-4B 规格与显存需求](https://apxml.com/models/qwen35-4b)；[Intel Core Ultra 边缘 AI 白皮书](https://builders.intel.com/docs/networkbuilders/heterogeneous-ai-powerhouse-unveiling-the-hardware-and-software-foundation-of-intel-core-ultra-processors-for-the-edge-1742810919.pdf)；[Ultralytics YOLO × OpenVINO 实测](https://www.ultralytics.com/blog/running-ultralytics-yolo-models-on-intels-ai-pc-with-openvino)；[Intel NPU 实测分析（Medium）](https://medium.com/@zlodeibaal/all-you-need-to-know-about-intel-npu-837ff5186423)
